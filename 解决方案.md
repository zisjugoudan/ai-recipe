方案结论
最适合当前 Flutter + SQLite + Local-first 架构的方案，是使用版本化、可迁移、可流式处理的备份归档包。
用户看到的文件扩展名为：
.airecipe-backup
内部采用标准 ZIP，但不直接复制 SQLite 数据库，也不使用“单个巨型 JSON + Base64 图片”。这样可以避免备份格式与当前数据库表结构、设备路径和平台实现绑定。
建议归档结构：
manifest.json
data/
  recipes.v1.ndjson
  categories.v1.ndjson
media/
  index.v1.ndjson
  sha256/
    ab/
      <完整sha256>.<ext>
1. 全量备份范围
每次点击备份都必须导出全部菜谱，不提供部分选择，包括：
正式、草稿、归档和回收站菜谱。
分类及分类关系，包括空分类。
食材、步骤、标签、收藏状态。
创建时间、更新时间、版本号、软删除等字段。
所有封面和菜谱配图。
当前已经存在的菜谱来源字段。
首版明确不包含：
冰箱库存和库存变更。
购物清单、制作记录、最近浏览。
运行中的导入任务、烹饪会话和计时器。
设置、API Key、Token、Cookie。
OCR 模型、缓存和临时文件。
以后这些能力可以作为新的 Dataset 加入，不需要破坏现有菜谱备份格式。
2. 可扩展机制
归档格式使用三套相互独立的版本：
备份容器 formatVersion。
每个 Dataset 自己的 schemaVersion。
当前应用 SQLite 的 schemaVersion。
通过以下扩展机制演进：
BackupDatasetAdapter：每种业务数据独立接入。
DatasetRegistry：注册菜谱、分类、媒体及未来数据集。
MigrationRegistry：按 v1 → v2 → v3 链式迁移旧备份。
requiredFeatures / optionalFeatures：区分必须理解和可提示跳过的数据。
dependsOn：声明数据集依赖。
未来增加库存、购物清单、营养分析、制作历史时，只需增加相应 Adapter 和 Dataset，不需要重写整个备份模块。
3. 导出策略
导出过程为：
一致性快照
→ 统计和空间预估
→ 校验数据关系和全部媒体
→ 流式写入 NDJSON
→ 图片按 SHA-256 去重
→ 生成 Manifest
→ 重新校验归档
→ 保存为 .airecipe-backup
如果任意必需图片缺失、不可读或哈希失败，备份必须失败并清理半成品，不能向用户交付不完整备份。
4. 导入策略
导入必须先进入隔离的 staging 区域，不能选完文件后直接修改当前库：
选择文件
→ ZIP 安全检查
→ Manifest 和版本兼容检查
→ 数据及媒体哈希校验
→ Schema 校验和迁移
→ 建立引用关系
→ 生成只读 ImportPlan
→ 用户确认导入方式和冲突策略
→ 写入媒体
→ SQLite 事务提交
→ 完整性复核
→ 清理 staging
ImportPlan 应展示：
新增、完全相同、可能重复和冲突数量。
媒体新增、复用、缺失和空间需求。
不兼容或准备跳过的数据集。
替换模式将删除的数据。
最终冲突策略和 ID 映射。
5. 合并、冲突和替换
默认使用合并导入：
同 UUID 且内容一致：跳过。
同 UUID 但内容不同：必须进入冲突处理，不能静默覆盖。
不同 UUID 但内容相同：标记为可能重复，默认仍保留。
冲突支持保留当前、使用备份、两者都保留。
“两者都保留”生成新 UUID，并重写关联引用。
高级“替换当前库”必须：
完成整个备份包预检。
显示将删除和替换的数据数量。
自动创建当前库的回滚备份。
验证回滚备份可读。
二次确认后才能执行。
如果回滚备份创建或验证失败，替换操作不能开始。
6. 关键前置条件
当前图片仍使用设备绝对路径保存，SQLite 和媒体文件系统也不能共享一个事务。因此，正式编码前必须先建立稳定的媒体资产契约：
mediaAssetId
SHA-256
MIME 和字节大小
稳定相对存储键
媒体引用关系
导入清理账本
这是保证跨设备恢复、图片去重和失败回滚可靠性的必要条件。
已落盘文件
核心方案：
docs\architecture\RECIPE_BACKUP_RESTORE.md
design\flows\RECIPE_BACKUP_RESTORE_FLOW.md
tests\acceptance\BACKUP-001-recipe-backup-restore-plan-2026-08-06.md
同步更新：
docs\product\AI食谱应用产品需求文档.md
docs\product\USER_STORIES.md
docs\architecture\README.md
design\README.md
tracking\BACKLOG.md
tracking\DECISIONS.md
tracking\RISKS.md
tracking\CURRENT.md
tracking\CHANGELOG.md
当前状态
本轮只完成方案，没有编写业务代码。
BACKUP-001 当前状态为 VERIFY，不是 DONE。
未执行自动测试、Flutter/Dart 静态分析、编译、构建、模拟器、真机或真实备份恢复。
功能、媒体资产契约和完整来源证据契约尚未实现。
加密能力目前只预留版本化扩展点，首版不承诺加密备份。
iOS 与 Android 跨平台创建和恢复尚未实机验证。
请按以下文件完成方案走查并回传结果：
tests\acceptance\BACKUP-001-recipe-backup-restore-plan-2026-08-06.md