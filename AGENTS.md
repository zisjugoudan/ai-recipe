# AGENTS.md — AI 食谱项目永久协作规程

> 生效日期：2026-07-27  
> 适用范围：本仓库中的产品、设计、技术探索、代码、测试和发布工作。  
> 协作成员：项目负责人（用户）与 Codex。  
> 规则等级：强制。功能紧急、对话中断、上下文丢失、切换模型或开始新任务，均不能跳过本流程。

---

## 1. 每次开始工作前必须读取

开始任何分析、设计或编码前，按顺序读取：

1. `AGENTS.md`：确认永久工作流程。
2. `tracking/CURRENT.md`：确认当前阶段、正在做什么、阻塞和下一步。
3. `tracking/BACKLOG.md`：确认任务编号、优先级、状态和验收目标。
4. `tracking/DECISIONS.md`：确认已经生效的产品与技术决策。
5. 与当前任务相关的产品、设计、架构和测试文档。
6. 如果任务涉及代码，再检查对应代码目录及其局部 `README.md` 或 `AGENTS.md`。

不得只依据聊天记录直接开始实施。聊天是输入渠道，本地文档才是长期事实来源。

---

## 2. 永久不变的跟进原则

### 2.1 本地单点事实源

本项目不依赖飞书、Notion、Linear、Figma 等云平台完成协作。所有可执行信息必须保存在本项目目录中，并可离线读取。

各类事实源如下：

| 内容 | 唯一事实源 |
|---|---|
| 产品需求 | `docs/product/AI食谱应用产品需求文档.md` |
| 用户故事与验收标准 | `docs/product/USER_STORIES.md` |
| 当前工作状态 | `tracking/CURRENT.md` |
| 全部任务 | `tracking/BACKLOG.md` |
| 已确认决策 | `tracking/DECISIONS.md` |
| 变更历史 | `tracking/CHANGELOG.md` |
| 风险与技术不确定性 | `tracking/RISKS.md`、`tracking/SPIKES.md` |
| UI/UX 设计交付 | `design/` |
| 技术架构与 API 契约 | `docs/architecture/`、`docs/api/` |
| 实际代码 | `code/` |
| 验收、回归与质量数据 | `tests/` |

同一事实不能在多个文件中维护不同版本。其他文档应链接到唯一事实源，而不是复制后各自修改。

### 2.2 需求变更单向流转

所有需求变化必须按以下顺序发生：

```text
用户提出或执行中发现问题
→ 更新产品文档或决策记录
→ 更新/新增任务卡
→ 更新设计或技术契约
→ 实现代码
→ 执行验收与回归
→ 更新进度和变更记录
```

禁止跳过文档直接改代码后再猜测需求。小型修复也至少要在 `tracking/BACKLOG.md` 留下任务或关联已有任务。

### 2.3 不使用口头决定

聊天中出现的产品、设计或技术决定，只要会影响未来实现，就必须在本轮工作结束前写入以下至少一处：

- 产品行为：产品需求文档。
- 技术选型或不可逆约束：`tracking/DECISIONS.md`。
- 尚未确认的问题：`tracking/BACKLOG.md` 或 `tracking/RISKS.md`。
- 当前任务进展：`tracking/CURRENT.md`。

没有落盘的决定不视为长期有效决定。

### 2.4 先验收定义，后实现

进入编码前，任务必须至少具备：

- 明确的任务编号。
- 用户价值或问题说明。
- 范围与不做内容。
- 可验证的验收标准。
- 依赖和风险。
- 涉及的产品/设计/API 文档链接。

若缺少这些信息，先补文档；不能以“开发时再看”为理由跳过。

### 2.5 AI 不确定性必须显式跟踪

涉及链接解析、OCR、ASR、LLM 的功能不能只判断“接口调用成功”。必须记录真实样本质量，包括：

- 来源平台与内容类型。
- 菜名、食材、用量、步骤的准确性。
- 低置信度字段是否正确标记。
- 用户保存前需要修改多少处。
- 使用的 Provider、模型、Prompt 和 Schema 版本。
- 失败原因与降级路径。

记录位置：`tests/ai-quality/AI_QUALITY_LOG.md`。

---

## 3. 固定执行流程

每个工作任务都执行以下七个阶段。阶段不能因任务紧急而删除，只能在小任务中合并记录。

### 阶段 1：定位

- 阅读必看文件。
- 确认任务编号；没有则创建。
- 确认当前事实源和相关文件。
- 检查是否与已有决策冲突。

### 阶段 2：分析

- 写明目标、范围、非目标和依赖。
- 识别是否存在技术不确定性。
- 高风险不确定性先建立 Spike，不直接承诺完整实现。
- 如需修改需求，先更新产品文档和决策记录。

### 阶段 3：设计

- 用户流程先于视觉美化。
- UI 可以使用 PNG、JPG、SVG、Markdown 或 HTML 原型。
- 每个设计必须覆盖规定状态，不允许只交付理想静态页。
- API 或数据模型变化先更新契约，再开始前后端实现。

### 阶段 4：实施

- 将任务状态改为 `DOING`。
- 代码只能写入 `code/`，测试只能写入 `tests/`，设计只能写入 `design/`。
- 实施中发现新问题时，立即记录到任务、风险或待确认项，不静默自行定义产品规则。
- 不在源码中提交真实 API Key、Token、账号或隐私数据。

### 阶段 5：验证

- Codex 默认不执行自动测试、Flutter/Dart 静态分析、编译或构建、模拟器测试、真机测试或人工验收，也不把静态代码阅读表述为“测试通过”。只有项目负责人在当前任务中明确要求 Codex 代测时，才可例外执行。
- Codex 必须把可复现的测试方法写入 `tests/acceptance/`，包括前置条件、操作步骤、预期结果、需要回传的实际结果和证据。
- 项目负责人按照测试方法执行，并把结果反馈给 Codex；Codex 收到反馈后负责归档验收结果、AI 质量记录和任务状态。
- 在项目负责人尚未反馈前，已实现任务进入 `VERIFY`，不得进入 `DONE`，不得写“测试通过”。
- 涉及 UI 的测试方法必须覆盖本任务相关的默认、空、加载、错误、极限和完成状态。
- 涉及 AI 的测试方法必须要求记录真实样本质量；涉及跨平台能力时分别记录 iOS 与 Android 的实际验证状态。

### 阶段 6：同步
完成或暂停工作前必须更新：

1. `tracking/BACKLOG.md` 中的任务状态。
2. `tracking/CURRENT.md` 中的当前进度、下一步和阻塞。
3. `tracking/CHANGELOG.md` 中的有效变更。
4. 必要时更新 `tracking/DECISIONS.md`、`RISKS.md`、`SPIKES.md`。

### 阶段 7：交付

对用户的完成说明必须包括：

- 完成了什么。
- 修改了哪些文件。
- Codex 未执行哪些测试，以及项目负责人应如何测试。
- 当前任务是等待验证、已由项目负责人验证，还是仍有阻塞。
- 仍有哪些限制、风险或下一步。

不得只说“已完成”而不提供可检查的落盘结果。

---

## 4. 任务状态规则

统一使用以下状态：

- `TODO`：已定义，尚未开始。
- `DOING`：正在实施；原则上同一时间只保留一个主任务。
- `BLOCKED`：存在明确阻塞，必须写明解除条件。
- `VERIFY`：实现完成，正在验收或等待真机/用户确认。
- `DONE`：验收标准全部满足并已同步文档。
- `CANCELLED`：明确取消，保留原因。

任务从 `TODO` 进入 `DOING` 前必须具备验收标准。实现完成但尚未收到项目负责人测试结果时必须进入 `VERIFY`。任务进入 `DONE` 前必须有项目负责人反馈的验证记录；Codex 自行推断、静态阅读或未执行的测试计划不能替代该记录。

---

## 5. 设计交付的固定标准

每个关键页面或流程至少覆盖：

1. 默认态。
2. 空状态。
3. 加载态。
4. 成功态。
5. 错误态。
6. 弱网或离线态。
7. 权限拒绝态。
8. 极限内容态，例如超长标题、大量食材、长步骤。
9. 游客态。
10. 登录态。

AI 导入流程额外覆盖：

- 剪贴板无链接与检测到链接。
- 获取内容、OCR、ASR、LLM 生成等阶段状态。
- 低置信度字段。
- 原文证据查看。
- 解析失败的文本、截图、视频和手动录入降级入口。
- 本地 Provider 不可用。
- 云端配额不足。
- 禁止上传图片时的隐私提示。

设计文件命名建议：

```text
<功能>-<页面或流程>-<状态>-v<版本>.<png|jpg|svg|html|md>
```

HTML 原型必须能使用本地资源离线打开，不得依赖只有登录后才能访问的云端链接。

---

## 6. 技术探索 Spike 规则

以下类型默认先做 Spike：

- 小红书、抖音链接解析稳定性。
- iOS/Android 分享扩展。
- 移动端后台长任务。
- Flutter 在数据库、分享、后台任务、安全存储和原生插件方面的关键能力验证。
- 本地 OCR 运行时、模型下载、内存和包体积。
- 手机连接 Ollama、LM Studio 等局域网 API。
- LLM JSON Schema 兼容性与降级解析。
- ASR 在真实视频和噪声环境下的准确率。

每个 Spike 必须输出：

- 问题和成功标准。
- 测试环境与样本。
- 尝试的方案。
- 数据结果。
- 结论：采用、拒绝或继续研究。
- 对产品、架构、排期和风险的影响。

Spike 结论存放在 `research/spikes/`，并更新 `tracking/SPIKES.md` 与 `tracking/DECISIONS.md`。

---

## 7. 目录边界

- 根目录仅放项目入口、仓库规则和通用配置。
- `docs/` 只存产品、架构和 API 文档。
- `tracking/` 只存执行状态、任务、决策、风险和日志。
- `design/` 只存流程图、线框图、UI、HTML 原型和设计评审。
- `research/` 只存技术探索和实验结论。
- `code/` 只存可运行代码和代码内说明。
- `tests/` 只存测试代码、测试样本、验收记录和 AI 质量数据。

新建文件前先判断归属，禁止为了方便把临时文件长期留在根目录。

---

## 8. 每次结束前的强制检查清单

- [ ] 产品行为变化已更新产品文档。
- [ ] 技术决定已更新决策记录。
- [ ] 任务状态与验收结果已更新；若项目负责人尚未测试，状态保持 `VERIFY`。
- [ ] 当前进度、下一步和阻塞已更新。
- [ ] 有效变更已写入变更日志。
- [ ] 已在 `tests/acceptance/` 写明项目负责人的测试方法、预期结果和回传格式。
- [ ] UI 状态覆盖完整，或已记录缺失项。
- [ ] AI 功能已记录真实样本质量，或说明当前没有可用样本。
- [ ] 未提交密钥、Token、账号或第三方受限内容。
- [ ] 用户可以从本地文件理解本轮发生了什么，不依赖聊天历史。

---

## 9. 修改本规程

功能需求、排期压力、工具变化和上下文丢失都不能自动改变本规程。

只有项目负责人明确提出修改协作流程时，才可以修改 `AGENTS.md`；修改时必须：

1. 在 `tracking/DECISIONS.md` 新增流程决策。
2. 在 `tracking/CHANGELOG.md` 记录变化。
3. 说明旧流程、修改原因、新流程和生效日期。
4. 不得为了单个任务临时绕过流程。

