# 当前工作状态

> 更新时间：2026-09-18

## 当前阶段

**2026-09-18 脱敏后同步全部工作区改动到 GitHub 公开仓库（OPS-005，VERIFY 待验收）**：远端 `zisjugoudan/ai-recipe` 此前是私有仓库，且自 2026-07-28 首次发布后只跟踪了 261 个文件；应用本体（`features/`、`domain/`、`application/`、`data/` 大量模块）、设计资产、原型、验收记录和 tracking 文档都停留在本地未提交（工作区共 6497 项改动）。本轮按 ADR-0043 完成：①**脱敏**——收款码（`design/assets/收款码/`、`code/apps/mobile/assets/payment/*.jpg`）、群二维码（`design/assets/交流群.jpg`、`code/apps/mobile/assets/community/*.jpg`）、视频工程（`视频剪辑/`，约 1 GB 含本地环境录屏）、JVM 堆转储（`*.hprof`，约 2 GB）、工具缓存（`.tmp/`、`.trae/`、`.workbuddy/`）与构建产物全部写入 `.gitignore`，逐项 `git check-ignore` 校验为 IGNORED；文本扫描确认无 `sk-`/`AIza`/`ghp_`/私钥块等凭证格式，无手机号与个人邮箱。②**就地说明**——新增 `code/apps/mobile/assets/payment/README.md` 与 `code/apps/mobile/assets/community/README.md` 说明所需文件名、参考源图与补齐方式（`pubspec.yaml` 与页面代码保持不变），根 `README.md` 增加「仓库内容边界」章节并同步过时的「当前阶段」描述，`解决方案.md` 中的本机绝对路径改为相对路径。③**提交者身份脱敏**——原 15 个提交的作者/提交者邮箱为 QQ 邮箱（QQ 号可直接反查联系人），转公开后会一并暴露，已用 `git filter-branch` 把全部 20 个提交改写为 GitHub noreply 地址（归属不变、去掉个人邮箱），改写前历史保留在本地分支 `backup/pre-email-desensitize`；④**提交与推送**——工作区改动按主题分批提交（脱敏配置 / 文档与验收 / 设计资产 / 应用实现 / 原生与资产）并推送到 `origin/main`，再用 GitHub API 把仓库可见性由 private 改为 public。**Codex 未执行 `flutter pub get`/`flutter analyze`/构建/单元测试/真机（ADR-0015）**，本轮不涉及应用功能行为变更；需要项目负责人复核的是公开范围本身（远端文件树不含收款码、群二维码、视频工程与堆转储）与克隆补齐步骤，方法见 `tests/acceptance/OPS-005-github-public-release-2026-09-18.md`。已知代价：全新克隆缺少三个被排除的资产文件，Flutter 构建会报找不到资产，须按两个 README 补齐。

**2026-09-04 开发历程分享视频成片渲染完成（VID-001，VERIFY 待验收）**：分镜 v2 放行后 Remotion 工程完成全部 28 镜实现。核心缺陷修复：Remotion `OffthreadVideo` 对竖屏录屏的纹理 stride bug 导致横纹花屏（重编码无效），全部视频组件（ClipScene/QuadSplit/OutroScene）切换原生 `<Video>` 后 2×2 交叉对照验证清晰。成片 `视频剪辑/开发历程分享视频/video/out/promo_final.mp4`（1920×1080/30fps/440s，带 BGM）+ 无 BGM 版渲染中。逐镜头终检 12 处关键镜头（四宫格/三卡/流程图/协作光标等）视觉核对全部通过，无条纹/花屏/遮挡；终检记录 `视频剪辑/开发历程分享视频/tests/终检记录_v1.md`。TTS 配音草稿 7 段已生成（`video/public/voice/`，S1/S2 因当时 API 请求格式错误未生成，不影响成片）。已知简化项：镜 21 终端为单终端代码窗（分镜原为三终端 3D）待项目负责人确认。下一步：项目负责人完整观看验收，确认口播方案（TTS 草稿 or 真人旁白替换），如有改稿需求则改 `src/index.tsx` 重渲染。

**2026-08-11 欢迎页按参考图视觉重设计（UI-WELCOME-001，VERIFY 待验收）**：已按项目负责人最新参考图完成红框品牌视觉区二次调整：①厨师帽、胡萝卜、番茄、辣椒、叶子使用容器宽度比例定位与缩放，标题区高度和浮动幅度也按宽度推导并限制范围；②「巴食」由 Flutter `CustomPainter` 以两套 13×13 手工点阵逐格绘制，不依赖系统中文字形或图片字标，并提供「巴食」图片语义；③主插画边框改为右下投影、深绿外轮廓、浅色框带、上左高光、下右压暗和内侧压边的多层像素立体相框；④气泡尾巴向上连接插画，右下爱心不裁切；⑤红框外特性列表、登录/游客入口、隐私说明和错误行为保持不变。已更新设计说明、Widget 测试定义与 `tests/acceptance/UI-WELCOME-001-welcome-page-redesign-2026-08-11.md`。**2026-08-11 追加（项目负责人反馈手工点阵与生成动画不好看）**：放弃手工点阵 `_PixelBrandPainter` 与 `_AnimatedPixelTitle` 波纹扩散动画，改用项目内置的 **Fusion Pixel 像素字体**（`assets/fonts/fusion-pixel.ttf`，OFL-1.1 可商用，支持简体中文）以 `Text` 渲染「巴食」，保留右下立体阴影与「拾味 · AI 菜谱」副标；`pubspec.yaml` 新增 `fonts` 注册 `family: FusionPixel`；测试 `find.text('巴食')` 由 `findsNothing` 改为 `findsOneWidget`。Codex 本轮未运行 `flutter pub get`、`flutter analyze`、`flutter test`、编译、构建、模拟器或真机测试；下一步由项目负责人在 320px、430px、600px 三类宽度及减少动态效果场景中验证像素字体渲染、元素构图和相框层次，并回传截图与结果。当前无代码实施阻塞，等待人工视觉与交互验收。

**2026-08-11 「我的」页新增「加入交流群」（COMMUNITY-001，VERIFY 待验收）**：因 Gitee 对仓库 raw 外链存在「涉嫌外连滥用」风控（匿名访问 403/404，官方申诉不保证解封），在线更新的数据源改为项目负责人指定 `version.json` 读取后，项目负责人决定先搁置更新分发、改为添加「加入交流群」入口。已实施：①`assets/community/qq_group.jpg`（源图 `design/assets/交流群.jpg`，1352×2405 竖版海报，153 KB，原图保留不处理）注册 pubspec.yaml；②新建 `lib/features/profile/community_page.dart`（`CommunityPage`：AppPageHeader「一起聊下厨那些事」+ 引导卡片「为什么加入交流群？」+ 群二维码海报卡片「进群方式」（按源图比例展示，点击进入黑底全屏预览 InteractiveViewer 双指缩放 + 像素关闭按钮）+ 底部进群提示卡片）；③`profile_page.dart`「支持我们」下方新增「加入交流群」入口（`openCommunityTile`，forum 图标绿底）。IDE 诊断无错误；Codex 未执行 `flutter pub get`/`flutter analyze`/构建/真机（ADR-0015）。等待项目负责人按 `tests/acceptance/COMMUNITY-001-join-community-2026-08-11.md` 验收：入口存在、海报清晰可扫码、全屏预览可缩放、离线可看（本地资产）。**在线更新（UPDATE-001）当前状态**：数据源已改为 `https://gitee.com/eb-Dog/delicious-food/raw/master/version.json` 宽容解析；但项目负责人实测仓库因「涉嫌外连滥用」被匿名访问限制，且确认无法让他人访问；已确认换用 GitHub 公开仓库方案（项目负责人选定），但 GitHub 国内访问不稳，最终方案待定（候选：国内对象存储腾讯云 COS/阿里云 OSS 最稳、Gitee 发行版附件次选、蓝奏云备选）。UPDATE-001 保持 VERIFY 待分发方案确认后改代码。

**2026-08-11 Gitee 在线更新（UPDATE-001，VERIFY 待验收）**：按项目负责人要求新增在线更新功能。数据源为 Gitee 公开仓库（项目负责人提供 https://gitee.com/eb-Dog/delicious-food，owner=eb-Dog, repo=delicious-food，公开无需 Token）。约定：Release 的 tag_name 为版本号（支持 v 前缀）、body 为更新说明、附件第一个 .apk 为下载源。已实现：①`domain/update/app_update.dart` 领域模型（`AppUpdateCheckResult`/`AppReleaseInfo`/`compareAppVersions` 语义化版本比较器）；②`features/update/app_update_service.dart`（`checkForUpdate` 调 `releases/latest`、`downloadApk` 带进度回调下载到临时目录、`installApk` 走平台通道）；③`features/update/update_page.dart` 检查更新页（显示当前版本 + 检查更新按钮 + 下载进度条 + 错误提示），发现新版本弹出像素风更新弹窗（版本号/当前版本/更新说明，按钮「以后再说」/「立即升级」，key：`updateDialogVersionText`/`updateDialogBodyText`/`updateLaterButton`/`updateNowButton`）；④「我的」页在「数据与存储」下方新增「检查更新」入口（key `openUpdateTile`）；⑤Android 原生：`ApkInstallMethodHandler.kt`（FileProvider + ACTION_VIEW 拉起系统安装器）、MainActivity 注册通道 `ai_recipe/apk_install`、AndroidManifest 增加 `REQUEST_INSTALL_PACKAGES` 权限 + FileProvider（`updatefileprovider`）+ queries（INSTALL_PACKAGE/VIEW+package-archive）；⑥依赖新增 `package_info_plus`（运行时读取版本号）；⑦单元测试 `test/domain/app_update_test.dart`（版本比较 7 项 + 结果模型 3 项）。全项目 IDE 诊断无错误；Codex 未执行 `flutter pub get`/`flutter analyze`/构建/单元测试/真机（ADR-0015）。**2026-08-11 修复（项目负责人反馈「当前版本未知 + 仓库没有发布任何版本」）**：①版本读取改多重回退（构建注入 `--dart-define=APP_VERSION` → package_info_plus → 回退 1.0.0），页面不再显示「未知」；②`releases/latest` 404 时二次请求仓库详情区分「仓库存在但无发布」与「仓库不存在/私有」两种文案；③新增 `test/features/update_service_test.dart` 6 项。待项目负责人确认：仓库是否公开、是否已创建 Release（含 .apk 附件）——私有仓库匿名 API 一律 404，需要改公开或提供只读 Token（代码未内置 Token，如需再补）。**2026-08-11 数据源变更（项目负责人指定）**：弃用 Release API，改为请求 `https://gitee.com/eb-Dog/delicious-food/raw/master/version.json` 读取更新配置；宽容解析候选键（版本号：version/latest_version/tag_name/latestVersion；说明：note/changelog/update_note/release_notes/body/description；APK：apk_url/apkUrl/download_url/downloadUrl/url/apk/apk_link/apkLink；发布页：html_url/page_url/release_page/homepage/project_url）；404/403 时二次请求仓库详情区分「仓库存在但缺 version.json」与「仓库不存在/私有」；测试 update_service_test.dart 全部改为 version.json 场景（8 项）。**2026-08-11 外链风控（项目负责人实测「当前仓库因涉嫌外连滥用」）**：Gitee 对 raw 外链有滥用风控（匿名 403/404、官方申诉不保证解封），项目负责人确认「别人无法访问」；已确认换 GitHub 公开仓库方案（推荐，raw/Release 附件直链无限制）但国内访问不稳，最终待定（候选：腾讯云 COS/阿里云 OSS 对象存储最稳、Gitee 发行版附件次选、蓝奏云备选）。等待项目负责人按 `tests/acceptance/UPDATE-001-gitee-update-2026-08-11.md` 验收：pub get + analyze 无 error、单元测试通过、「我的」页入口、「当前版本 1.0.0」、无新版提示/有新版弹窗/立即升级下载安装三场景（分发平台确认后实施下载）。

**2026-08-11 LLM 与识图设置结构重分类（SETTINGS-001，VERIFY 待验收）**：按项目负责人要求重新打散排列 LLM API 设置与识图引擎两个页面。确认结构（负责人选定）：「我的」页保持两个入口——「LLM 模型」与「识图引擎」。「LLM 模型」页（`llm_settings_page.dart`）集中两类 LLM 配置：菜谱生成 LLM（普通文本）+ 多模态 LLM 图片识别（从识图引擎页迁入，独立配置）；页面标题由「本地 LLM API」改为「LLM 模型」。「识图引擎」页（`ocr_settings_page.dart`）只保留 OCR 相关（图片识别方式 / 本地 OCR 模型 / 云端路线），移除多模态 LLM 配置表单。已移动 `_MultimodalLlmConfigSection` 等 6 个私有类/函数，清理 4 个 import、新增 3 个 import。IDE 诊断无错误；Codex 未执行 `flutter analyze`/构建/真机（ADR-0015）。等待项目负责人按 `tests/acceptance/SETTINGS-001-llm-ocr-restructure-2026-08-11.md` 验收：LLM 模型页含两个区块、识图引擎页仅 OCR、两类 LLM 配置可独立保存。

**2026-08-10 首页/冰箱/导航图标替换与 12 个默认分类（HOME-001，VERIFY 待验收）**：按项目负责人要求替换冰箱、菜品分类、快速导图与底部导航栏图标。新增 `tool/process_home_icons.py`：四角采样去底 + 保留设计素材原始颜色 + 64×64 正方形 + nav.png 自动切 8 张。生成资产 `assets/icon/home/`（fridge 4 张/categories 12 张/quick 4 张/nav 8 张），均已注册 pubspec.yaml。扩展 `PixelTab`/`PixelTabBar` 支持 `Widget` 图标，`app_shell.dart` 底部导航、`fridge_page.dart` 分区、`home_page.dart` 快速导入与分类列表、`recipe_library_page.dart` 分类筛选全部改用新图标。新增 `default_recipe_categories.dart` 定义 12 个默认分类，并在 `RecipeLibraryUseCases.listCategories()` 中幂空自动创建。图标文件均 64×64、全套约 100 KB。2026-08-11 追加：项目负责人反馈底部导航栏选中态不应显示绿色激活方框边框，已先移除 `Border.all`；随后又要求去掉绿色背景，已将 `Material` 颜色改为 `Colors.transparent`。选中态现在无任何背景填充/边框，仅通过 active/inactive asset 图标切换与深绿文字表达。Codex 未执行 `flutter analyze`/构建（ADR-0015）；等待项目负责人按 `tests/acceptance/HOME-001-home-icons-and-categories-2026-08-10.md` 验收：静态分析无 error、冷启动默认分类 12 个、各页面图标无背景残留、底部导航选中态无背景填充/无边框。

**2026-08-10 「我的」页新增「支持我们」赞赏页（PROF-001，VERIFY 待验收）**：按项目负责人要求把 `design/assets/收款码/`（微信.jpg 1213×1213、支付宝.jpg 1260×1890）打包为应用资产 `assets/payment/wechat_qr.jpg` / `alipay_qr.jpg`（pubspec.yaml 已注册），新增 `features/profile/support_page.dart` 赞赏页——`AppPageHeader`（感谢你的支持）+ 感谢语卡片（赞赏自愿、核心功能永远免费）+「扫码赞赏」两张收款码卡片（按源图比例展示 + 平台标签 + 点击放大）+ 底部提示；点击收款码进入全屏黑底预览（PageView 左右滑动切换 + InteractiveViewer 双指缩放 + 像素风关闭按钮，参考菜谱详情 `_FullscreenImageGallery`）。「我的」页「隐私与数据」区块末尾新增「支持我们」入口（`openSupportTile`）。产品文档 4.4「我的」已补充该条目。IDE 诊断无错误；未运行测试/构建/真机（ADR-0015）。**已修复（2026-08-10 项目负责人运行时报 "No Material widget found"）**：`SupportPage` 为独立路由缺少 `Scaffold`（无 Material 上下文），已在 build 外层补 `Scaffold(body: SafeArea(...))`。**追加（开发者工具隐藏入口 + 头像改程序图标）**：开发者工具区块默认隐藏，连续点击头像 6 次（1.5 秒窗口）切换显示/隐藏并提示；头像改显示程序图标 `assets/brand/app_icon.png`（源图与启动图标同源，已注册 pubspec）。等待项目负责人构建后人工验收：入口跳转、两张收款码清晰显示、放大预览可缩放/滑动、头像显示程序图标、连点 6 次出现/隐藏开发者工具。

**2026-08-10 应用启动图标替换为「巴食」图标（BRAND-002，VERIFY 待确认）**：按项目负责人要求把应用图标改成 `design/assets/图标.png`。首轮已用一次性脚本（保留于 `code/apps/mobile/tool/gen_launcher_icons.dart`）生成 20 个文件。**2026-08-10 追加：项目负责人再次提出换图，检查发现源图已于 16:28 更新为 2048×2048（生成文件时间戳 15:55 早于源图），已用新源图重新生成全部文件**——沙箱内 `dart run` 持续无输出超时，改用 PowerShell/System.Drawing 等价脚本（居中裁剪正方形 + HighQualityBicubic 缩放）：①Android 5 个 mipmap `ic_launcher.png`（48/72/96/144/192，24bppRGB 无透明，已验证尺寸与格式）；②iOS `AppIcon.appiconset` 15 个尺寸（20×20@1x ~ 1024×1024@1x），合成纯白底去除 alpha（15 文件全部无 alpha 已验证）；无乱码路径残留文件；`tool/gen_launcher_icons.dart` 注释已同步。未运行测试/构建/真机（ADR-0015）。等待项目负责人重新构建后在桌面/应用管理器确认新图标，确认后 `BRAND-002` 进入 `DONE`。

**2026-08-10 应用名称统一为「巴食」（BRAND-001，VERIFY 待确认）**：按项目负责人要求把应用名称改成「巴食」，并完成 `BRAND-001` 首次盘点与统一——①Android `AndroidManifest.xml` `android:label` → `巴食`（桌面显示名）；②iOS `Info.plist` `CFBundleDisplayName` → `巴食`；③Flutter `app.dart` MaterialApp title → `巴食`，欢迎页品牌标题「拾 味」→「巴 食」（字距保留、眉题 `TASTE PIXEL · AI RECIPE` 不变）；④HTML 原型（index.html/app.js/prototype.css/README.md）与 design-docs 全部「拾味」→「巴食」。未改动：pubspec 包名 `ai_recipe`、iOS CFBundleName/bundle id、历史验收记录与任务描述中的遗留引用。Flutter 代码与测试目录已无「拾味」断言、原型目录已无残留；未运行测试/构建/真机（ADR-0015）。等待项目负责人重新构建后确认 Android 桌面应用名与欢迎页品牌标题显示为「巴食」，确认后 `BRAND-001` 进入 `DONE`。

**2026-08-10 「巴食」严格二维像素美术提示词（DESIGN-008，VERIFY）**：按项目负责人“其他图标不使用 IP”的要求，提示词已升级为 v1.3，并收紧官方 IP 使用边界：①仅 App 启动图标、品牌核心符号、欢迎页 Hero、通用菜谱无封面 4 项品牌资产允许上传并参考 `design/assets/IP.png`；②状态插图 12 条、功能入口 10 条、冰箱/库存 10 条、分类 default 12 条，共其他 44 条基础 Prompt 全部禁止上传、参考或生成 IP，只用物体、食材和符号表达语义；③底部导航首页、菜谱库、冰箱、我的 4 组 / 8 枚 default / active glyph，以及分类 12 张 active 同样禁止 IP；④所有非品牌资产禁止水豚、人物、动物角色、厨师、五官、手脚、拟人表情、吉祥物或角色轮廓，并继续执行正视 / 正交、有限色板、实色块、清晰方形像素台阶及无 3D / 无透视 / 无厚度 / 无渐变 / 无软阴影 / 无抗锯齿约束。已同步总规范、分组 Prompt、6 个 Image 2 直投 txt、索引与 v1.3 验收方法。Codex 未调用 Image 2、未生成图片、未执行 Flutter/Dart 测试、分析、构建、模拟器或真机测试；当前等待项目负责人回传 `bottom-navigation-glyph-state-proof-v1.3.png`、`category-breakfast-v1.png`、`category-breakfast-active-v1.png`、`state-fridge-empty-v1.png` 四张首轮样张，收到通过反馈前保持 `VERIFY`。

**2026-08-09 烹饪模式按钮亮色与暗色同长修复（COOK-001 追加，VERIFY 待复测）**：按项目负责人要求修复"上一步/下一步与计时器部分的按钮亮色部分比暗色部分短"——根因是按钮/面板主体底色为半透明（`0x1AFFFFFF` 10% 白、`0x14FFFFFF` 8% 白、`_panel`/`_panelStrong` 6%~9% 白），半透明底叠在深绿背景上几乎不可见、只有右下 2~4px 像素阴影可见，视觉上暗色（阴影）比亮色（主体）长。已修复：新增不透明实色 `_panelSolid`（`0xFF41584C`，比背景亮一档的深绿），替换全部带阴影的 `_CookingPixelSurface` 半透明底色——`_CookingNavButton`（上一步/下一步）、`_CookingIconAction`（暂停/继续/完成计时）、`_TimerCard`（计时器卡片）、当前步骤面板 `currentCookingStepPanel`、`_CookingStateIcon`（完成状态大图标）、`_CookingMiniButton`；删除不再使用的 `_panel`/`_panelStrong` 常量。现在亮色主体为实色、阴影只在右下 2px 露边，亮色与暗色同宽。文件 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015），等待项目负责人复测。

**2026-08-08 烹饪模式按钮阴影与空食材提示修复（COOK-001 追加，VERIFY 待复测）**：按项目负责人要求①`_CookingNavButton`（上一步/下一步、启动本步计时、自定义本步计时）阴影偏移从 compact 3px / 非 compact 4px 统一减小为 `Offset(2, 2)`；②`_CookingNavigation` 上一步/下一步按钮最终方案：保持 `Expanded` 整行宽度（与阴影同宽，项目负责人明确按钮宽度要与阴影一致），阴影随按钮尺寸，不再 Center 窄化；③`_CookingNotice`（空食材提示）去掉 `_CookingPixelSurface` 矩形背景/边框/阴影，改为纯文字 + 信息图标。文件 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015），等待项目负责人复测。

**2026-08-08 底部弹层风格统一（REC-001 追加，VERIFY 待复测）**：按项目负责人要求统一底部弹层像素风——在 `app_theme.dart` 的 `ThemeData` 新增全局 `bottomSheetTheme`：背景 `AppColors.card` 纸张卡片色、`PixelCutOutlinedBorder`（`PixelCut.sm` 切角 + `line2` 1.5px 描边）、`elevation: 0`（无阴影）、`showDragHandle: false`。这样所有 `showModalBottomSheet`（菜谱详情分享弹层、冰箱批次弹层、导入粘贴正文弹层）自动统一为像素面板外观，无需逐处重复设置。文件 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015），等待项目负责人复测。

**2026-08-08 菜谱详情"复制"改"分享"（REC-001 追加，VERIFY 待复测）**：按项目负责人要求把 `recipe_detail_page.dart` 的「复制菜谱」替换为「分享菜谱」——①底部操作栏 `copyRecipeButton` 改为 `shareRecipeButton`（`Icons.ios_share_rounded`，tooltip「分享菜谱」）；②更多菜单 `_RecipeDetailMenuAction.copy` 改为 `share`，菜单项「复制菜谱」→「分享菜谱」；③原 `_copy()` 方法改为 `_share()`：点击后 `showModalBottomSheet` 弹出 `_ShareSheet` 像素风底部弹层，标题「分享『菜谱名』」，列出两个入口——「分享为图片」（副标题：把菜谱生成一张图片）与「其他方式」（副标题：系统分享、链接分享等），两个入口均为**预留占位**（点击关闭弹层并提示「该分享方式即将上线，敬请期待。」，`_ShareOptionTile` 复用 `PixelSurface`）；④后端 `copyRecipe` Facade/UseCase 保留（公共 API 与测试仍在用）。文件 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015），等待项目负责人复测。

**2026-08-08 首页最近浏览改横排（UI-001 追加，VERIFY 待复测）**：按项目负责人要求把首页「最近浏览」从竖排列表改为**横排滚动小卡片**——`RecipeSummaryCard` 新增 `tile` 模式（`_buildTileCard`）：正方形封面（AspectRatio 1:1，无图回退色块+图标、有图本地文件填充）在上，标题（最多 2 行）+「所需时间 · 人份 · 食材数」元数据在下；`home_page.dart` 用横向 `ListView.separated`（卡片宽 132、间隔 10、高度 205，展示全部最近浏览）。原竖排 `compact` 模式与「我的收藏」竖排列表不变。文件 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015），等待项目负责人复测。

**2026-08-08 烹饪模式排版改版（COOK-001 追加，VERIFY 待复测）**：按项目负责人要求重排 `cooking_mode_page.dart`——①头部改为「关闭按钮 | 进度 | 菜谱名称」（1:2:1 三列，进度整行居中、菜谱名右对齐）：进度用最多 5 个小方块（`_CookingStepProgress`，已完成=主色填充、当前步=琥珀描边、未来=半透明），上方标注详细步骤顺序 `STEP 当前/总数`；**超过 5 步时小方块为滑动窗口跟随当前步骤移动（当前步尽量居中）**；保留原 `cookingStepProgress` key 与 `STEP 2 / 3` 文本格式，兼容既有测试；②原「STEP 行 + 步骤面板 + 本步提到的食材」合并为**一个分组面板** `_CookingStepGroup`：详细步骤 → 所需时间/火候/温度/炊具 chips → 分割线 → 所需食材（替换原 `_StepPanel` 与「本步提到的食材」独立区块，原「COOKING MODE」眉题移除）；③分组结束后续接「TIMERS · 计时器」与启动/自定义计时按钮不变；④删除不再使用的 `_StepPanel` 类。关键 key 保留：`currentCookingStepText`、`currentCookingStepPanel`、`cookingTimer-*`。视图文件 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015），等待项目负责人复测。

**2026-08-08 推荐卡操作按钮改小（RECO-001 追加，VERIFY 待复测）**：按项目负责人要求把推荐卡内「查看详情」（OutlinedButton）「开始烹饪」（FilledButton）高度改小——`minimumSize` 高 30、水平内边距 12、字号 12、`VisualDensity.compact`。**再按项目负责人要求调字号 16 + 补上下边距 6**。视图静态分析无诊断；未运行构建/真机（ADR-0015）。**另按项目负责人要求去掉分组标题（旗标带）3px 阴影**。

**2026-08-08 推荐页折叠与"更多缺失"对齐原型（RECO-001 追加，VERIFY 待复测）**：按项目负责人要求——①推荐结果分组「现在就能做」「优先清库存」改为可折叠（与「缺 1/2 样」一致，点旗标带标题展开/收起，`collapsible: true`）；②每张推荐卡片内新增「食材」区折叠开关（`_RecommendationCard` 改 StatefulWidget，默认展开，可收起食材标签与匹配依据，只留标题/匹配度/操作按钮）；③「更多缺失」完全按原型 `details.fold` 重做 `_FoldableMore`（pxc-sm 切角 + line2 1.5px 描边 + summary 整行可点 + 展开箭头 + fold-body 列出缺失菜谱行 + 底部提示，默认折叠，去掉原「查看更多」子按钮与 `_visible` 分页）。视图文件通过静态分析无诊断。未运行构建/真机（ADR-0015），等待项目负责人复测。


**2026-08-08 优化推荐匹配关系（RECO-001 追加，ADR-0038，VERIFY 待复测）**：按项目负责人六条默认原则重写 `InventoryRecommendationUseCases.recommend()` 主循环为三阶段**全局批次分配**——①身份匹配（YES）按需求属性分面数从多到少优先分配并占用批次（`usedBatchIds`，精确库存优先：五花肉批次优先五花肉、普通猪肉满足猪肉）；②家族/替代（MAYBE）只召回候选不代表已拥有（占用单一批次、不计命中/缺失/临期、数量不统计）；③缺失/规格冲突。需求去重由按基础 ID 改为按「基础+分面」（猪肉/五花肉为独立需求）；数量只统计最终分配批次；未收录基础食材（如鸡蛋）退化为同名精确匹配。新增 `_RecipeRequirement`/`_isYesFor`/`_requirementKey`/`_facetCount`/`_unitOf`，删除 `_bestMatch`/`_BestMatch`。测试：推荐组 30 项全部通过（新增 3 项原则用例，修正 3 项旧断言「未收录食材无批次→缺失」）；既有 `aggregates same-name`/`marks used up` 2 项失败位于 `loadAggregated`/`markUsedUp`，与本次 recommend 改动无关。未运行构建/真机（ADR-0015）；等待项目负责人复测：冰箱同时有五花肉与普通猪肉、或同一份辣椒面对多个辣椒类需求时，卡片不再重复命中、数量不再虚高。


**2026-08-07 RECO-001 推荐页「缺 N 样」分组整组折叠（项目负责人反馈）**：`fridge_recommendation_view.dart` 的 `_ResultsSectionState` 新增整组折叠状态 `_collapsed`，`initState` 默认折叠「缺 1 样」「缺 2 样」；`_buildGroup` 新增 `collapsible` 参数，折叠时只显示标题；`_GroupTitle` 支持 `collapsed`/`onToggle`，标题行可点击展开(↑)/收起(↓)，展开后保留「查看更多」逻辑。**再按项目负责人反馈参考原型 HTML 改为「方案A · 像素旗标带」**：对照 `prototype.css#.rg-ribbon` 用 `_GroupRibbon`（实色带+切角+3px 投影 + 计数徽标 + 虚线延伸 `_RibbonLinePainter`）替换原 `_GroupTitle`，分组配色 现在就能做=绿/可能可以做=琥珀/库存不足=红/缺 1·缺 2=琥珀/优先清库存=蓝；折叠功能保留。**微调**：旗标带标题底部间距 6→14，拉开与下方第一张卡片的距离。全项目 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015）；等待项目负责人复测。

**2026-08-07 BUG-009 修复「识别图片导入后确认草稿」framework 断言崩溃（VERIFY）**：项目负责人反馈确认草稿时崩溃，日志含 `_elements.contains(element) is not true` 与 `Looking up a deactivated widget's ancestor is unsafe`，并有系统返回键事件。根因（只读审查 `import_draft_review_page.dart` 与 `import_progress_page.dart`）：async 方法在 await 后使用 context 做导航/弹窗前仅用 `if (!mounted)`（`State.mounted`）防护，但路由退场过渡期 Element 已 deactivate 而 `State.mounted` 仍为 true，此时 `Navigator.of(context)`/`showDialog(context)` 触发断言。修复：将「await 后使用 context 做 push/pop/showDialog」前的判断改为 `if (!context.mounted) return;`（deactivated 期为 false）。涉及：确认页 `_save`/`_discard` 的 pop；进度页 `_openReview`（confirmAll pop / 单草稿保存后 pop / catch 回退 push/pop）、`_pushReviewPage` 开头、`_showMultiDraftPicker` 的 showDialog。纯 setState 保留 `State.mounted`。全项目 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015）；等待项目负责人复测「识别图片导入→确认草稿」保存/放弃/按返回键不再崩溃。

**2026-08-07 LLM 识别/结构化生成强制保持原文语言（项目负责人反馈"识别时 LLM 私自把语言改了，应强制按所给内容语言"）**：①多模态转录固定提示词 `multimodal_image_enriching_import_content_processor.dart#defaultPrompt` 强化——显式要求「必须保持与图片文字完全相同的语言输出，禁止翻译、禁止改用其他语言；图片是中文就输出中文、日文就日文、英文就英文，不因提示词语言而改变输出语言」；②结构化生成 `recipe_generation_prompt.dart#_systemPrompt` 增加语言指令——所有文本字段（标题/简介/备注/食材名与处理/步骤/贴士）与源文本同语言，禁止翻译（即使指令本身为英文）。全项目 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015）；等待项目负责人用多语言配图复测。

**2026-08-07 慢速 LLM 服务下「最终结构化生成」固定 120 秒超时失败（BUG-008，VERIFY）**：项目负责人反馈链接只有一张图片，单图识别成功（82 秒）后「最终结构化生成」报 `kind=timeout`（耗时 120025ms）。根因：`requestTimeout` 默认 120 秒、两处设置页超时档位仅 30/60/120，慢速本地模型（Ollama/LM Studio 等）生成完整菜谱 JSON 超出 120 秒即被 `.timeout()` 主动中断。修复：①LLM 配置页 `llm_settings_page.dart` 与识图引擎多模态区 `ocr_settings_page.dart` 超时档位扩展为 30/60/120/180/300/600 秒；②结构化生成与多模态识别超时错误文案补充「调大『请求超时』」指引；③**诊断辅助**（应项目负责人"llm 响应不可能这么慢，加一个测试结构化生成选项看耗时"）：LLM 设置页新增「测试结构化生成」区块——输入任意文本走真实结构化生成链路（`LlmSettingsUseCases.testStructuredGeneration` 复用 `RecipeGenerationPromptBuilder`，Facade 透传 `testLlmStructuredGeneration`），展示耗时与生成字符数，用于判断超时瓶颈在 LLM 本身还是图片识别等其他环节。**本轮追加修复（项目负责人反馈「测试 30 秒、实际 120 秒超时」排查）**：定位并修复一个关键不一致——`LlmSettingsUseCases._buildConfiguration` 构造测试/保存用配置时未继承已保存的 `reasoningMode`（恒为 fast），「测试结构化生成」prompt 也未跟随存储配置；若存储配置为 `deep`（PERF-001，无设置页入口不可见），实际导入会发送 `reasoning_effort: high` 明显变慢，测试却恒以 fast 运行，造成「测试快、导入慢」假象。已修复：保存/测试均继承 `existing.reasoningMode`、测试 prompt 显式传 `reasoningMode: existing?.reasoningMode`（与导入链路一致）、`LlmStructuredGenerationResult` 新增 `reasoningMode` 字段并在设置页耗时文案展示（如「· 模式 fast」）；另确认除推理模式外，测试输入为手输短文本、实际导入 prompt 为「原文+整图 OCR 转录全文」（`sourceCharacterCount` 最大 40000），长文本生成耗时显著更高属正常现象。测试：`llm_settings_page_test`（600 秒档位、结构化生成携带文本并展示耗时、空输入提示）、`ocr_settings_page_test`（多模态档位含 600 秒）、`llm_settings_use_cases_test`（`testStructuredGeneration` 组 5 项：成功/空文本/超时映射/deep 继承/保存保留 deep）。全项目 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015）；等待项目负责人按 `tests/acceptance/BUG-008-request-timeout-options-2026-08-07.md` 复测（新增用例 7 推理模式显示、用例 8 用真实 OCR 转录文本复现耗时）。

**2026-08-07 烹饪模式无时长步骤自定义计时并同步菜谱（COOK-001，VERIFY）**：项目负责人反馈"烹饪界面有些步骤没有计时，加一个自定义的；用户自定义后同步到菜谱"。已实现：①领域 `RecipeLibraryUseCases.updateRecipeStepDuration(recipeId, stepId, durationSeconds)`——校验时长 > 0、步骤属于当前菜谱；`Recipe`/`RecipeStep` 不可变且无 copyWith，只重建目标步骤（其余字段与步骤保留）整体重建 `Recipe` 并递增 `localVersion` 后 upsert；②`AiRecipeBackendFacade` 透传（`_recipeOperation` 包装，异常映射稳定错误）；③烹饪模式页无时长（`durationSeconds` 为 null/0）步骤显示「自定义本步计时」按钮（key `customStepTimerButton`）；点击弹像素风 `_CustomTimerDialog`（分钟/秒两输入框，透明 Dialog + `PixelSurface` 切角/描边/投影，控制器随弹窗释放；确认 key `confirmCustomTimerButton`、取消 key `cancelCustomTimerButton`）；确认后先 `updateRecipeStepDuration` 同步菜谱再 `addCookingTimer` 创建「步骤 N」计时器，SnackBar"已创建计时器，并将时长同步到菜谱。"；非法/≤0 时长拦截且不关弹窗。**2026-08-07 反馈调整**：按项目负责人"计时的页面不需要提示文案"删除计时器空态「暂无计时器…」与无时长步骤「本步骤未设置时长…」两处提示。测试：domain 3 项 + facade 2 项 + 烹饪页 widget 3 项已更新（含 2 分 30 秒→150 秒创建并同步、取消无副作用）。文档：产品 5.4.4、DECISIONS ADR-0037、BACKLOG COOK-001 已同步。全项目 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015）；等待项目负责人按 `tests/acceptance/COOK-001-custom-timer-sync-2026-08-07.md` Android 复测。

**2026-08-07 首页快速导入「拍照选图/剪贴板」报「导入任务暂时无法保存，请稍后重试」（BUG-007，VERIFY）**：根因已确认——`import_tasks.source_platform` 的 SQLite CHECK 约束只允许 `('xiaohongshu','douyin')`，而本地图片/文本导入占位 URL 与通用网页导入经 `ImportSourceLink.parse` 解析后的平台值为 `web`，insert 违反约束抛 DatabaseException，被 Facade `catch (_)` 吞掉替换成该文案。已实施修复：schema v12 表重建迁移（`_createImportTaskTable` 放宽约束 + `_rebuildImportTasksTable` 建临时表→复制实际列→删旧表→重命名→重建索引，幂等且兼容 v9 前缺列旧库）+ 迁移测试新增 web 平台任务经 repository 真实路径写入/读取与旧数据保留断言。**2026-08-07 后续修复（项目负责人反馈「解析后变成了从链接导入，给的是本地链接，应直接多模态识别」）**：三处根因——①`startWithFallback`/`runWithContent` 守卫只允许 failed/cancelled 内容运行，拍照选图新建的 queued 占位任务无法直接用本地图片内容运行（抛 ImportTaskTransitionException）；②导入进度页 `_SourceCard` 无条件显示占位 URL 与「网页链接」标签，用户误以为走了链接导入；③`_resolveLocalImagePlan` 自动模式默认本地 OCR 优先（ADR-0029 旧规则），与「直接多模态识别」期望不符。已修复：①queued 任务允许直接以本地内容运行（进入 extracting，跳过 fetching 网页抓取）；②占位任务显示「本地图片导入/文本整理导入」真实来源语义，不暴露内部占位链接；③自动模式改为多模态优先，多模态未配置/不可用回退本地 OCR（ADR-0029 修订，DECISIONS.md 已更新）。测试已更新：domain（queued 可 startWithFallback）、runner（queued 占位任务直接处理内容无 fetching）、facade（新增多模态优先正面用例 + 多模态不可用回退 OCR + 路由全不可用→providerRouteUnavailable 不暴露路径；queued 允许、running 拒绝）。全项目 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015）；等待项目负责人按 `tests/acceptance/BUG-007-local-image-import-task-save-2026-08-07.md` Android 复测（拍照选图、剪贴板文本、通用网页链接三类入口 + 升级迁移旧库 + 拍照选图不再显示网页链接且直接多模态识别）。

**2026-08-07 备份导入「替换恢复后什么数据都没有」调查 + 合并预检 bug 修复（BACKUP-004）**：项目负责人反馈替换恢复导入后菜谱库/首页/回收站全部为空，但预览「新增 > 0」、完成页「写入 > 0」。已新增真实 SQLite + 真实 ZIP 集成测试 `test/application/backup_import_integration_test.dart`：①「替换恢复」用例（创建备份 → 清库 → 替换导入 → 通过 `SqliteRecipeRepository` 读取）**通过**——证明「写入后读取」链路在代码层面真实落库可读（含食材/步骤/图片落盘/分类关系/回收站软删除）；②「合并导入」用例**复现并修复真实 bug**：预检计划正确（add=0/skip=2），但 `executeImport` 却返回 writtenRecipes=2——根因 `_resolveMergeRecords` 只按 conflicts 决定写入，预检标为「同 UUID 同内容跳过」的记录无 conflict 被当真新增写入。修复：`BackupImportPlan` 新增 `skippedRecipeIds`/`skippedCategoryIds`，`_buildPlan` 收集跳过 ID、`_resolveMergeRecords` 先跳过再处理冲突。修复后两个用例全部通过；备份模块回归（导入/导出集成 + 备份页）**10 项全部通过**。**项目负责人复测后根因已确认（本轮修复）**：①替换恢复更早的大备份（两千多条含约两千条回收站）**没有提示完成直接回到导入页**——审阅页 `_buildReviewing` 不渲染 `_error`（只有选择文件页渲染），执行抛错回到审阅页后错误不可见；②「替换后全空」两个根因——`_tryRestoreFromRollback` 把**已取消的导入 token** 传入恢复管线，恢复第一步（staging）即中断，库停留在清库后空状态；替换流程 `clearLibrary`（独立事务）与 `applyImport` 两步之间失败/进程被杀无法自动恢复。已修复：审阅页渲染错误 + 取消/替换失败引导文案；恢复改用独立 `restoreToken`；`applyImport` 新增 `clearExisting` 参数在**同一事务内清库+写入**（原子）；替换成功仍停在完成页供用户点「完成」返回。新增「替换执行中取消原库保留」「替换导入取消安全」集成测试（`_SelfCancellingToken` 确定性模拟事务中途取消），导入集成测试 5 项 + 备份模块回归 23 项全部通过。请项目负责人用那份 2000+ 条备份复测「替换恢复」——再失败时审阅页会直接显示具体原因。**2026-08-07 进度反馈增强（本轮）**：项目负责人反馈预检加载期缺视觉反馈、大量菜谱导入时进度一直 0 后突然成功。根因：`BackupImportProgress` 调用点都没传 processed/total（fraction 恒 0），staging/媒体复制/写库均无逐条上报。修复：领域接口（`BackupArchiveStageRequest`/`stageMediaToCoverStorage`/`applyImport`）新增 onProgress；`DeviceBackupArchiveReader` 按记录+媒体总量逐条上报、媒体写入器按引用数上报、`SqliteBackupImportRepository.applyImport` 按分类+菜谱逐条上报；用例桥接 staging/creatingRollback（桥接导出进度）/preparingMedia/applying；`PixelProgressBar` 新增 `indeterminate` 动画（无总量阶段不再停 0%），导入页预检卡片加进度条、执行阶段无百分比显示「处理中…」。新增「导入进度逐步上报」集成测试（中间值 + 到达 100% + 单调不减），导入集成 6 项 + 备份模块回归 24 项全部通过。等待项目负责人复测大备份导入的进度反馈。**2026-08-07 删除性能优化 + 导入返回刷新（本轮）**：项目负责人反馈①回收站删除太慢；②删除测试数据很慢；③导入完成后加载数据量突增无 UI 反馈，用户可能以为导入失败。根因与修复：①`emptyRecipeTrash` 原全量加载完整聚合 + 逐条永久删除（每条独立事务）→ 新增 `permanentlyDeleteRecipesByIds`（单事务 500/批 IN 删除，ON DELETE CASCADE 清子表）+ `emptyRecipeTrash` 改 `listTrashSummaries()` 轻量取 id 批量删；②`ProfilePage._clearTestRecipes` 原全量 + 逐条软删除 → 新增 `softDeleteRecipesByIds`（单事务分批 UPDATE `deleted_at`）+ 轻量摘要 keyset 分页收集 id；③导入页返回后主界面不刷新（用户看旧数据误以为失败）→ `BackupPage` 新增 `onDataChanged` 回调，`_openImport` 返回后触发刷新 + 重载预估，ProfilePage 透传。验证：sqlite_recipe_repository_test 11 项（新增 520 条跨批永久/软删除用例）+ 备份模块回归 32 项全部通过；profile_page_test 3 项「databaseFactory not initialized」为既有测试基建缺失，与本次改动无关。等待项目负责人复测：回收站清空 2000 条应秒级完成；导入大备份返回主界面应立即看到新数据。

`BACKUP-002`/`BACKUP-003`（VERIFY）：备份格式核心与创建全量备份导出已实现。`.airecipe-backup`（标准 ZIP）导出完整链路：领域契约（`backup_archive_constants/errors/cancellation_token/manifest/records/repository`，三套独立版本，SHA-256 内容寻址，严格序列化，未知 dataset/未来版本拒绝）、单事务一致性快照（含软删除菜谱）、Isolate ZIP 组装写入器（`.partial` 写入 → 重读复核 → 原子重命名，失败清理半成品）、`BackupExportUseCases`（预估/阶段进度/去重/取消检查点）、Facade 新增 `estimateBackup`/`createFullBackup` 并映射稳定错误码、组合根输出目录默认 `documents/backups`、UI「我的 → 数据与存储 → 创建全量备份」（默认/空/加载/进度/取消/成功摘要/错误重试全覆盖）。测试代码已写（`test/domain/backup_manifest_records_test.dart`、`test/application/backup_export_integration_test.dart`）但未运行。2026-08-07 项目负责人首次 `flutter run` 编译失败，6 处错误已全部修复（`_HashedMedia.sourcePath`、`ZipFileEncoder` 导入与 archive 4.0.9 实际 API、const 字符串插值、块体 collection-if、`IngredientImportance.fromWireName` 参数类型、进度弹窗 `_loadEstimate` 回调；`archive` 提升为直接依赖）；空白页根因（`PixelStatCard` 在 Column 中 `Expanded` 无界 flex）已修复（新增 `expand` 参数），预估容忍脏媒体路径。`flutter analyze` 备份相关目录无问题、全 lib 无 error。Codex 未运行测试/构建/真机（ADR-0015）；等待项目负责人重新运行并按 `tests/acceptance/BACKUP-002-full-backup-export-2026-08-06.md` 验收（含归档结构解压核对、取消与媒体缺失失败清理、空库与幂等）。

**2026-08-07 修复「归档复核失败：清单内容不一致」**（项目负责人真机创建备份时提示）：根因三个——①Dart `List` 未重写 `==`，复核用 `listEquals` 的 List 身份比较比较 manifest 记录与重读记录恒不等 → `device_backup_archive_writer.dart` 复核改逐字节 `_bytesEqual`；②`expectedCount` 在循环中重复累计 datasets 次数 → 改为 `requiredEntries.length + mediaFiles.length` 只计一次；③`hashedMedia` 按 path 去重导致同内容不同路径媒体被重复归档且清单计数不符 → `backup_export_use_cases.dart` 改 `uniqueMediaBySha` 按 sha256 内容去重（manifest `media.count`/`mediaFiles`/`_buildStatistics` 均用 unique）。另将 `mediaEntryPath` 升级为严格 64 位十六进制校验。回归验证：`test/domain/backup_manifest_records_test.dart` + `test/application/backup_export_integration_test.dart`（含新增复核/去重用例）**33 项全部通过**；`flutter analyze --no-pub` 本轮改动文件无任何问题。等待项目负责人实机重测创建备份不再报归档复核失败。

**2026-08-07 备份「另存为」出口（ADR-0036）**（项目负责人反馈"备份创建成功但不知道保存在哪"）：备份默认落在应用私有文档目录（Android 普通文件管理器不可见）。已实施：①「上次备份已创建」区域新增「另存为（导出到其他位置）」按钮，调用 file_picker `saveFile`（SAF）把备份复制到用户选择位置（如下载），成功提示「备份已另存到：<路径>」、取消不提示、失败提示「另存失败，请重试。」；②成功摘要展示文件名、体积与完整路径；③`BackupPage` 新增可注入 `saveBackupFile`/`estimateBackup`/`initialResult`（测试注入 fake，默认行为不变），上次备份结果区域移到 ListView 底部独立于预估状态展示；④复用导入侧 file_picker，无新增依赖。验证：`test/features/backup_page_test.dart` **4 项全部通过**（成功复制/取消/失败/无结果态）；`flutter analyze` 改动文件无问题；备份回归 14 项通过。等待项目负责人真机确认：创建备份 → 另存为到「下载」→ 系统文件管理器可见该 `.airecipe-backup` 文件。**2026-08-07 修复「另存失败」**：根因 file_picker 8.x 在 Android/iOS 上 `saveFile` 必须传 `bytes`（插件只把传入 bytes 写入用户所选文件，不传则 Dart 端抛 ArgumentError）→ `_defaultSaveBackupFile` 改传 `readAsBytes()` 并移除错误的 `File.copy` 到伪路径；Android 返回路径为拼接伪路径，提示改为「备份已保存到所选位置」，其余平台仍显示完整路径。已知限制：bytes 经平台通道整体传输，超大备份（数百 MB）有传输上限，归 BACKUP-006。

`BACKUP-004`/`BACKUP-005`（VERIFY）：备份导入（预检、合并与冲突）与替换恢复/回滚已实现。预检流水线：staging（ZIP 安全校验：Zip Slip/Bomb/重复路径/非法路径/大小上限 + Manifest 版本兼容 + 数据逐行 Schema + 媒体 SHA-256 校验）→ 预检当前库（只读）→ 生成 `BackupImportPlan`（新增/跳过/冲突/可能重复/缺失媒体/替换影响预览），全程不改变当前库。默认合并：同 UUID 同内容跳过、同 UUID 不同内容进冲突（保留当前/用备份覆盖/两者保留，keepBoth 换新 UUID 并重写分类与菜谱引用）、不同 UUID 同内容标可能重复、仅名称相似不自动合并；两阶段提交（媒体先落盘→SQLite 单事务，失败回滚媒体）。替换模式（BACKUP-005）：替换范围预览 + 二次确认 + 强制创建并验证 `ai-recipe-rollback-<时间戳>` 回滚备份 + 清库（事务）+ 全部写入 + 孤儿图片清理；清库后写入失败回滚本次媒体并尽力从回滚备份恢复，回滚备份文件保留供手动恢复。清理账本：成功/失败/放弃预检均清理 staging 会话目录，启动时 `cleanupAbandonedImportSessions` 扫除遗留目录。实现位置：`backup_import_plan.dart`（冲突/模式/影响预览/copyWith）、`backup_import_repository.dart`、`backup_record_hashing.dart`（规范化内容哈希，排除图片设备路径/updatedAt/localVersion）、`device_backup_archive_reader.dart`、`device_backup_import_media_writer.dart`、`sqlite_backup_import_repository.dart`、`backup_import_use_cases.dart`（预览/冲突解决/执行/清理）、Facade 新增 `previewBackupImport`/`resolveImportConflict`/`resolveAllImportConflicts`/`discardImportPreview`/`executeBackupImport`/`cleanupAbandonedImportSessions`、组合根注入（staging 默认 `documents/backup-import/`）、UI `backup_import_page.dart`（选择文件→预检→策略→执行→完成，5 阶段进度/取消/成功摘要/回滚路径）+ 备份页「从备份导入」入口 + `file_picker` 依赖。2026-08-07 构建修复：`file_picker` 升级 `^8.3.0`（3.0.4 无 namespace 不兼容 AGP 8.11）；选择文件改为 `FileType.any` + 手动校验 `.airecipe-backup` 扩展名（Android 无法映射该自定义 MIME）；`flutter build apk --debug` 在 `C:\AIM`（junction → `code/apps/mobile`）下构建成功产出 app-debug.apk（impellerc 无法写中文路径；sqlite3 hook 下载经代理 `127.0.0.1:7897` 一次成功后已缓存）。`flutter analyze` 无 backup error。**2026-08-07 修复「替换恢复大备份失败无提示、替换后全空」**（项目负责人复测：小备份替换恢复成功但加载慢、完成后仍停加载页；大备份两千多条替换恢复无提示直接回导入页）：①`_tryRestoreFromRollback` 误传已取消导入 token → 恢复第一步即中断 → 库停在清库后空状态，改为内部独立 `restoreToken`；②替换流程两步式清库（clearLibrary 独立事务 → applyImport）中途失败/进程被杀无法恢复 → `applyImport` 新增 `clearExisting` 参数在同一事务内清库+写入（原子，失败全回滚）；③审阅页不渲染 `_error` 导致失败无提示 → 审阅页渲染 `AppErrorState` + 取消/替换失败引导文案；④替换成功仍停在完成页（用户点「完成」返回见结果）。新增集成测试：`_SelfCancellingToken` 确定性模拟事务中途取消断言原库保留、executeImport 预取消安全；导入集成 5 项 + 备份模块回归 23 项全部通过。等待项目负责人按 `tests/acceptance/BACKUP-004-backup-import-2026-08-07.md` 验收。回滚备份保留期限与跨平台规模归 `BACKUP-006`（TODO）。

`BACKUP-001`（DONE）："全量菜谱备份与恢复"产品、架构和交互方案已由项目负责人走查通过（2026-08-06，7 项维度全部通过，附 3 项实施前细化建议：统一媒体失败语义、回滚备份保留期限定值、阐明 minimumReaderVersion 与 formatVersion 关系）。方案确定 `.airecipe-backup`（标准 ZIP）+ Manifest + 版本化 NDJSON Dataset + SHA-256 内容寻址媒体；每次备份覆盖正式、草稿、归档、回收站全部菜谱及分类、关系、食材、步骤、标签、收藏、版本/软删除和图片；导入默认合并、冲突不静默覆盖，高级替换前强制回滚备份，并要求 staging、安全预检、ImportPlan 和两阶段提交。方案、流程与走查结果见 `docs/architecture/RECIPE_BACKUP_RESTORE.md`、`design/flows/RECIPE_BACKUP_RESTORE_FLOW.md`、`tests/acceptance/BACKUP-001-recipe-backup-restore-plan-2026-08-06.md`。

`IMPORT-010`（VERIFY）：解析过程实时显示当前操作详情（progressDetail 全链路）。用户反馈"在解析的时候也要同步当前的信息——获取到了文案、几张图片、是单个草稿还是多个草稿的、正在弄成草稿的叫什么"。已实施：①领域模型 `ImportTask` 新增只读展示字段 `progressDetail`，`advance` 增加可选 `progressDetail`（进度/阶段不变但详情变化时生成新版本、不触发进度倒退校验），`_copy` 用哨兵 `_notProvided` 区分"不传=保留/传 null=清空"，`fail/cancel/retry/recoverAfterRestart/startWithFallback` 清空、`markNeedsReview/reassignResultRecipe/complete` 保留；②`ImportPipelineProgressCallback` 增加可选位置参数 `[String? detail]`（旧两参回调完全兼容），`AdvanceImportTask` 与 runner `_advance`/onProgress 透传；③SQLite schema v11 新增 `import_tasks.progress_detail` 列（幂等 `_addColumnsIfMissing`），仓库读写同步；④各处理器上报：多模态"正在识别 N 张图片中的文字/第 x/N 张图片"、多图分组"正在分析 N 张图片是否为同一道菜→识别为 N 份独立菜谱，正在生成草稿"、并发生成"正在生成第 x/N 份菜谱草稿→第 x/N 份草稿已生成→全部草稿已生成，正在收尾"、结构化生成"正在整理识别到的内容，生成菜谱草稿→AI 已返回草稿，正在校验菜谱格式→草稿《标题》已生成，正在保存"；⑤进度页运行中提示行改动态显示 `progressDetail`（无详情回退静态文案）。全项目 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015）；等待项目负责人复测（方法见 `tests/acceptance/IMPORT-010-progress-detail-2026-08-06.md`）。**本轮修复**：复测日志发现多草稿并发生成偶发"导入失败·不可重试"（`groups=0|1|2 草稿数=3`、第一份 Generate 46188ms 后失败）——根因是并发窗口 2 下两个组 `onProgress` 并发写任务撞 SQLite 乐观锁（`ImportTaskWriteConflictException`）或进度交错回退（`ImportTaskTransitionException`），被 runner catch-all 吞成 unknown+不可重试。已修复：处理器 `_generateDraftsConcurrently` 用 `reportChain` 串行队列串行化全部进度上报（源头消除并发写）+ runner `_advance` 对写冲突/进度倒退有限重试（`_progressWriteRetries=3`，基于最新版本取 max 兜底，取消时优雅返回）。待复测并发多草稿导入不再失败。

`PERF-002`（VERIFY）：按《解决方案.md》第二批完成图片导入链路性能优化（ADR-0031）——①**任务级图片缓存**避免重复下载：新增 `TaskScopedImageCache`（实现 `OcrRemoteImageStager`，按 https URL 去重），由 `DeviceImportTaskRunnerFactory.create()` 创建并同时注入多模态处理器 stager 与 `LlmRecipeGenerationProcessor` 的 `remoteCoverImageStager`，识别与封面保存共用一次下载，处理器链结束后 `_CacheReleasingProcessor` 统一 `disposeAll()`；②**多模态识别有限并发流水线**：串行 `for` 改为并发 2 的 `_runConcurrent` 工作池（图片 1 下载完立即识别、图片 2 同时下载），结果按下标保序，单张识别失败（非取消）折叠为 null 不拖垮整批仅标记部分内容，取消立即终止，全部失败给出可重试提示；③**识别前压缩重编码**：新增 `ImageRequestCompressor`（image 包 `decodeImage`→`bakeOrientation` EXIF 修正→超长边按需等比缩放→`encodeJpg` 质量 85；普通照片长边 2048px，长宽比≥2.5 的文字密集截图保留 4096px；压缩失败或无体积收益保留原图，只影响上传字节，不改动磁盘原图仍用于封面）；④**深度思考开关**：领域新增 `LlmReasoningMode{fast,deep}`/`LlmReasoningCapability`，`LlmConnectionConfig.reasoningMode`（默认 fast，`toJson/fromJson` 兼容旧配置），`LlmProvider.reasoningCapability`，OpenAI-compatible `reasoning_effort`（deep→high）与 Gemini `thinkingConfig` 映射；图片转录（多模态）始终快速、多图分组判断显式 `fast`、最终结构化生成跟随用户开关（`RecipeGenerationPromptBuilder.build` 接收 `reasoningMode`，`LlmRecipeGenerationProcessor` 传 `config.reasoningMode`）。**本轮按项目负责人反馈修复**：多草稿生成串行（实测 55+56+28s≈139s）改为并发窗口 2 的 `_generateDraftsConcurrently`（结果保序、进度共享 `lastEmitted` 取最大值保证单调不减），项目负责人确认"现在快了一点"。全项目 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015）；等待项目负责人按 `tests/acceptance/PERF-002-import-pipeline-performance-2026-08-06.md` 复测。iOS 未验证。

`PERF-001`（VERIFY）：按《解决方案.md》完成菜谱列表与推荐性能优化（ADR-0030）——①新增 `RecipeSummary`/`RecipeSummaryPage`/`RecipeSort`/`RecipeCursor` 摘要分页模型，`listRecipeSummaries` 单条 SQL（LEFT JOIN 聚合食材数）替代逐条 `_loadRecipe`（约 5001 次 → 1 次）；`countRecipes`/`countRecipesByCategory`/`getRecipeSummariesByIds` 供总数与首页分类计数；②菜谱库页重写为 `SliverGrid` 虚拟化 + keyset 分页（40 条/页、滚动加载、`_loadGeneration` 防串页、旧列表顶替不闪动画），排序由 SQL 承担，打开详情按 id；③首页 `HomeSnapshot` 改用 `List<RecipeSummary>` + `RecipeSummaryCard`，不再全量读取；④推荐改走 `listRecipesForRecommendation`（主记录+食材 2 次 SQL），匹配移入 `RecommendationExecutor`（生产 `Isolate.run`，测试注入直算避免 FakeAsync 挂起）；⑤推荐页 250ms 防抖 + 最新请求优先 + 每组 Top 10 与"查看更多"；⑥标准化器缓存 + 别名/规格词典 Map 化 + 家族/上下位哈希索引；⑦schema v10 复合索引迁移；⑧新增推荐匹配性能测试。全项目 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015）；等待项目负责人按 `tests/acceptance/PERF-001-recipe-list-recommendation-performance-2026-08-06.md` 复测并回传 `[Perf][RecipeLibrary]`/`[Perf][Recommendation]` 性能数据。iOS 未验证。

`回收站加载性能优化`（2026-08-07，修复项目负责人反馈「回收站加载的太慢了」）：根因是回收站页原用 `listRecipes` 全量加载每条菜谱（含食材/步骤/图片，N+1 次查询），菜谱多时明显卡顿。已实施：①领域新增轻量只读模型 `TrashRecipeSummary`（id/title/deletedAt）；②`RecipeRepository` 接口新增 `listTrashSummaries()`，`SqliteRecipeRepository` 用单条 SQL（`WHERE r.deleted_at IS NOT NULL ORDER BY r.deleted_at DESC, r.id DESC`，只选 id/title/deleted_at）替代逐条全量加载；③UseCase/Facade 透传 `listTrashSummaries`；④`recipe_trash_page.dart` 改为 `Future<List<TrashRecipeSummary>>` + `_reload()` 走轻量摘要，卡片显示 `deletedAt` 时间；⑤配套 fake 仓库补实现。测试基建补齐：trash 页 Widget 测试需 `sqfliteFfiInit()+databaseFactory=databaseFactoryFfi`（组合根建 AppDatabase）、`SharedPreferencesAsyncPlatform.instance = InMemorySharedPreferencesAsync.empty()`（`DeviceMultimodalLlmConfigRepository` 硬编码构造）、`accessibilityFeaturesTestValue = FakeAccessibilityFeatures(disableAnimations: true)`；同时 `PixelFloat` 增加系统减少动态支持（`MediaQuery.disableAnimationsOf(context)` 时停 ticker 直接返回静态子组件，遵循 UI-002「减少动态直达终态」，也避免无限动画让 `pumpAndSettle` 永不结束）。验证：trash 页 3 项 + 菜谱库用例/集成共 **19 项全部通过**；`flutter analyze --no-pub` 本轮改动 15 个文件无任何问题（70 个既有 issue 全为历史遗留）。等待项目负责人实机确认回收站加载不再卡顿。

`IMAGE-001` 识图引擎已实现并进入 `VERIFY`：①"OCR 设置"改名"识图引擎"，页顶新增"图片识别方式"全局单选（自动/仅 OCR/多模态 LLM，SQLite v8 迁移 `image_recognition_mode`，保存后立即生效）；②新增独立"多模态 LLM 图片识别"配置区（Provider/API 地址/模型/Key/超时，独立存储 key 与菜谱生成 LLM 分离）；③`MultimodalLlmProvider`（OpenAI-compatible image_url + Gemini inline_data，图片转 base64，8MB/类型校验，纯文本 generate 复用现有 Provider 供测试连接）+ `MultimodalImageEnrichingImportContentProcessor`（安全暂存→读字节→多模态识别→并入证据→LLM 生成草稿，固定中文提示词，错误统一中文映射）；④`ImportImageRecognitionRoute {disabled,ocr,multimodal}` 替换旧 `ImportOcrRoute`，`_resolveDefaultPlan` 按设置+能力自动解析（auto：多模态→OCR→纯文本），`runImportWithLocalImage` 同步按设置；⑤`AppCapability.multimodalLlm` 能力（配置+Key 就绪）；进度阶段文案改"正在识别图片内容"；入口与设置页标题改"识图引擎"。新增 ADR-0027。未运行测试/构建/真机（ADR-0015）；等待项目负责人按 `tests/acceptance/IMAGE-001-vision-engine-2026-08-04.md` Android 复测（识别方式三选一保存、多模态配置独立保存/测试、选多模态/OCR 分别导入含菜谱信息图片验证草稿与证据、自动回退、未配置中文引导、超时/断网稳定中文）。iOS 未验证。

**BUG-006 多模态连接诊断与识图融合（2026-08-05，VERIFY）**：项目负责人确认“测试连接”始终显示超时，但实际不是普通网络超时。已按 `VISION_RECOGNITION_STRATEGY.md` 完成 P0-1/P0-2 实施：①P0-1 分阶段诊断——`LlmProviderErrorKind` 扩充 HTTP 405/413/415/408/504/409/423/400/422、首字节、正文空闲、SSE、图片断言分类；domain 新增 `llm_diagnostic.dart`（阶段/记录/分层时限）；`HttpLlmTransport.sendDiagnostic` 流式观察响应头/首字节/正文空闲（连接 8s、首字节 15s 基础·45s 图片、空闲 15s、总时限沿用用户配置），失败按阶段返回不再统一“超时”，取消优先于超时；内置 256×256 标准诊断图（白底黑字 “AI RECIPE 314”+红色方块，SHA-256 固定）+ `vision_probe.dart` JSON 断言；`MultimodalLlmProvider.diagnose` + Facade 拆三入口（`checkMultimodalBaseConnection` 视觉模型拒纯文本标记“文本能力未确认”/`checkMultimodalImageCapability` 标准图断言/`testMultimodalImage` 真实图片），1×1 占位图删除；设置页三入口按钮 + 分阶段结果卡（阶段/耗时/HTTP/中文 messageKey/建议）。②P0-2 证据融合——`ImportTextFragment.sourceType`、`ImportContentWarning.visionIncomplete/ocrIncomplete`、`ImportImageRecognitionRoute.ocrAndMultimodal`（auto 两路就绪时融合）、`VisionFusionImportContentProcessor`（独立执行/分别结算/一路失败保留另一路/两路失败有正文继续·无证据不生成空草稿/冲突按来源保留）、工厂组装融合链路、`RecipeGenerationPromptBuilder` 片段带 sourceType+来源优先级说明、草稿确认页证据面板来源标签。新增 `vision_probe_test` 与 `vision_fusion_import_content_processor_test`。全项目 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015）；等待项目负责人按 `tests/acceptance/BUG-006-multimodal-diagnostics-and-vision-fusion-2026-08-05.md` 复测三入口矩阵与融合矩阵。

**IMAGE-002 多图多模态识别与多草稿（2026-08-05，VERIFY，ADR-0029）**：按项目负责人方向实施：①多模态提示词改为“忠实转录图片全部文字”（不再做视觉观察描述），转录产物 `sourceType=ocr` 并保留 `sourceMediaOrder` 供按图分组，“图片没有文字”视同空结果；②多草稿数据契约——`ImportRecipeDraftResult.additionalRecipeIds`、`ImportTask.additionalResultRecipeIds`（`markNeedsReview`/`reassignResultRecipe` 支持附加草稿）、SQLite v9 迁移 `import_tasks.additional_result_recipe_ids`（JSON 数组）、`getImportDraft(taskId, recipeId:)` 按草稿读取、确认页支持指定草稿、进度页多草稿逐份确认（保存一份即返回，其余保留）；③新增 `MultiImageDraftSplitProcessor`——纯文本 LLM 轻量分组判断（independent→每图独立草稿 / combined→合并单草稿 / mixed→按图号分组），按组拆分转录证据逐份生成多草稿，判断失败回退全部合并不丢内容；④路由——链接 auto 改多模态优先（不再自动融合 OCR），新增 `_resolveLocalImagePlan` 供手动选图/拍照导入（本地 OCR 优先，未安装/不可用回退多模态）。**本轮修复复测失败 "导入失败 Only queued import tasks can be run. 尝试1/3·不可重试"**：根因一为 `MultiImageDraftSplitProcessor` 分组循环进度公式不含组号，组间进度必然倒退触发"任务进度不能倒退"；根因二为 `ImportTaskRunner._latestCancellationOrRethrow` 把真实错误掩盖成误导性消息。已改为每组进度落在 [0.6,0.9] 独立子区间单调递增，并让 `_latestCancellationOrRethrow` 原样重抛原始异常。**另按项目负责人反馈重做多草稿弹窗**：像素风容器替换 Material AlertDialog，每项点击整行进编辑确认、右侧删除图标单项删除（二次确认、移入回收站可恢复）、左侧勾选框多选 + 底部"删除所选 (N)"批量删除、"全部确认"一键添加；新增后端 `deleteImportDrafts`（部分删除剩余草稿重排、删光取消任务）。**再修"点了返回却被添加菜谱"**：①"全部确认"一键批量入库无确认，误触即添加——加像素风二次确认；②点进某草稿确认页再返回时弹窗消失——恢复"未保存返回后回到多草稿弹窗继续处理其他草稿"，返回本身不添加菜谱。**另实现菜谱库长按选择模式**（项目负责人反馈"菜谱库长按进入选择模式"）：网格卡片长按进入选择（绿色描边+对勾角标），选择模式下点按切换选中、收藏按钮隐藏、页头"已选择 N 项"+取消，底部浮出操作栏（加入收藏 / 删除所选 N 项，批量删除二次确认入回收站）；`RecipeCard` 增加 `onLongPress`/`selected`（`PixelSurface` 已支持 `onLongPress` 与 `onTap` 由同一 InkWell 处理保证互斥稳定）。**另做菜谱库性能优化**（项目负责人反馈"点收藏后进入加载动画太慢"）：收藏改为乐观更新（`favoriteOverride` 本地立即切换，失败回滚），不再触发整页刷新与加载动画；列表改"缓存顶替"（`_cachedData` 顶替，仅首次无缓存显示加载动画）。**另新增菜谱库 1000 条数据读取性能测试**（`test/performance/recipe_library_perf_test.dart`，`sqflite_common_ffi` 真实 SQLite，插入 1000 条并打印全量/收藏/搜索读取耗时）。全项目 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015）；等待项目负责人重新构建后按 `tests/acceptance/IMAGE-002-multimodal-multi-image-2026-08-05.md` 复测（多图独立多草稿/多图合成单草稿/单图回归/手动图 OCR 回退/取消与 9 图上限/重启恢复，以及弹窗像素风、单项删除、多选删除、删光后任务取消、返回后弹窗保留、"全部确认"二次确认不再误添加），并复测菜谱库长按选择（长按进入选择模式、多选、加入收藏、删除所选、取消退出、回收站恢复）与收藏即时响应（不再出现整页加载动画），运行性能测试回传耗时，回传 `[AIRecipe][Grouping]` 日志。

`OCR-003` 已实现并进入 `VERIFY`：OCR 设置页新增"测试 OCR"功能——点击按钮打开系统图片选择器，选一张图片由本地 OCR 直接识别（不上传），输出识别文本、耗时（ms）、文本块数、平均置信度、模型版本与语言；失败显示红色稳定中文错误（模型未安装/不可用、超时、取消、网络不可用等统一映射），空文本明确提示，测试期间防重复点击。已按项目负责人反馈修复入口缺失：按钮原只在"模型包已安装"分支内，现改为模型包卡片内常驻显示，未安装/下载中/安装失败时点击给中文引导提示（不打开选择器），已安装则照常选图识别。**已按项目负责人反馈接入真实模型包清单（ADR-0026）**：`lib/app/ocr_model_catalog.dart` 定义 `ocrPpocrV5MobileZhManifest`（PP-OCRv5 mobile zh，RapidAI/RapidOCR 官方 ModelScope ONNX，SHA-256 与字节大小均为 2026-08-04 真实下载实测，约 21.5 MB）与 `ocrTrustedModelHosts`；`app.dart` 组合根传入受信主机、`profile_page.dart` 传入 Manifest，"下载本地模型"按钮恢复可用，不再显示"正式模型包尚未发布"。**已修复下载失败（ModelScope GET 302 重定向被拒 → `HttpOcrModelDownloadClient` 手动逐跳仅 HTTPS 跟随）与识别失败（PP-OCRv5 rec 输出类别数 = 字典 +2 而非 +1 → `PaddleOcrEngine`/`OcrCtcDecoder` 校验范围放宽为 +1..+2、尾随额外类跳过，Kotlin 测试同步更新）**。链路：`OcrTestResult` 领域模型 + `AiRecipeBackendFacade.testLocalOcrImage`（复用 `localOcrProviderBuilder` 本地 Provider）+ composition root 注入。未运行测试/构建/真机（ADR-0015）；等待项目负责人安装后按 `tests/acceptance/OCR-003-ocr-settings-test-2026-08-04.md` Android 复测（下载安装 → 状态推进 → 测试 OCR 选图识别输出文本与耗时，并回传真实样本质量：菜名/食材/用量/步骤准确性、耗时、需人工修改数）以归档 AI 质量日志。iOS 未验证。

`UI-002` 第十三轮冰箱拖拽与逐格冷冻动效交接方案已完成，代码尚未实施，任务保持 `DOING`：动画触发边界固定为“最终目标接收 → 库存保存成功 → 目标 chip 已挂载 → 唯一到达事件被消费”，长按、拖拽开始、经过候选区、无效释放、同区排序和非冷冻区之间移动均不播放颜色动画；冷冻/解冻改为二维独立小格逐个出现/消失，拖拽反馈与原位占位保持静态；同名批次和连续拖动用 dragSessionId、moveEventId、stableItemId、源区/目标区和动画类型隔离，不再使用“名称 + 250ms”时间窗口去重；保存失败恢复原位且不播成功动画，系统减少动态时直达终态。详细交接见 `design/flows/FRIDGE_DRAG_AND_FROST_ANIMATION_HANDOFF.md`，工程师实施后由项目负责人按 `tests/acceptance/UI-002-fridge-drag-frost-animation-2026-08-04.md` 验收。本轮未修改应用代码、未执行测试；下一步由工程师按交接方案改造 `fridge_page.dart`。

`FRIDGE-001` 冰箱与按食材推荐菜谱的产品规划已补充 UI 与交互方向并保持 `VERIFY`：一级导航确定为“首页 / 菜谱库 / 冰箱 / 我的”，添加/导入保留全局 FAB；冰箱包含“库存 / 推荐”，库存采用冷藏区、冷冻区、常温区和其他区的轻拟物分区；推荐先由用户选择本次想优先使用的库存食材，再按已具备、缺 1 样、缺 2 样和优先清库存匹配本地正式菜谱；卡片区分已选命中、冰箱已有但未选择和缺失项；下厨后自动生成预计消耗建议，但只有用户确认才扣减；AI 生成新菜谱作为后续独立入口。业务代码尚未开始，等待项目负责人按 `tests/acceptance/FRIDGE-001-fridge-recommendation-planning-2026-08-03.md` 完成文档走查。中文名称、同义词和单位换算由 `SPK-008` 后续验证。

`IMPORT-006` 链接导入自动获取配图作为封面已实现并进入 `VERIFY`：`LlmRecipeGenerationProcessor` 生成草稿时把公开内容配图经 OCR-002 安全暂存下载并复制到 `recipe_covers/<recipeId>/`，写入草稿 `images`/`coverImage`（最多 9 张，首图即封面）；单张失败 best-effort 跳过、全部失败草稿仍生成；丢弃/重新生成草稿时同步清理旧封面目录。本轮按项目负责人反馈连续补强：草稿确认页直接展示配图并修复保存清空 `images`；确认页与编辑页食材按 AI 分组分段展示、可常显编辑与快捷 chip、分组标题可删除、同组自动归并、标题即时刷新；编辑页食材与步骤可长按拖拽排序；配图下载失败在确认页提示"笔记配图暂未获取"。配图失败根因已实锤：真机 Logcat 确认全部 403，DNS 解析 IPv6 在前且客户端固定连第一个地址（IPv6），CDN IPv6 边缘节点对非浏览器请求 403，而 Windows 同节点/IPv4 节点均 200、真机浏览器可打开；已实现 IPv4 优先 + 地址级故障转移（连接失败或 403 依次尝试下一地址），并修复 cover_image 残留远程 URL、残缺图片 URL 过滤到最终层。等待项目负责人重新构建后按 `tests/acceptance/IMPORT-006-remote-covers-2026-08-03.md` Android 真机复测（重点确认 `[AIRecipe][RemoteImage] 已连接` 走 IPv4 且下载成功）。iOS 未验证。

`IMPORT-007` 链接导入启用本地 OCR 已实现并进入 `VERIFY`：`AiRecipeBackendFacade.runImportTask` / `dispatchPendingImports` 执行计划自动解析（本地 OCR 就绪则 `ocr: local`，未就绪回退纯文本路径）；图文内容由 `OcrEnrichingImportContentProcessor` + `RemoteStagingOcrProvider`（OCR-002 远程安全暂存 + 本地 OCR）把图片识别成文字，与页面正文按顺序合并为同一批证据后统一交给 AI 生成菜谱；OCR 链路错误文案全部中文化。等待项目负责人安装后按 `tests/acceptance/IMPORT-007-link-import-ocr-2026-08-03.md` Android 复测。iOS 未验证。

界面文案全中文化收尾已完成：首页英文日期改为动态中文日期，快速导入、游客徽标等眉题/徽标全部中文化；“我的”页移除了显眼的“游客模式 · 本地优先”横幅，该描述只保留在游客身份徽标内。全局扫描确认 UI 层无大写英文界面文本残留，IDE 只读诊断无错误。该改动未运行测试、分析器或构建（`ADR-0015`），随下一次 Android 安装一并查看。

`BUG-004` 已由项目负责人完成 Android 模拟器验收并进入 `DONE`：首页“有 N 个未完成的导入”会打开最近创建的原持久化任务，不误入“添加菜谱”页；实测数量在只打开并返回时保持 `12 → 12`，没有创建同链接重复任务，取消后数量减少，强制停止并重启后仍能恢复原任务，也没有多余草稿或正式菜谱。“成功完成并保存后数量减少”因 `BUG-003` 无法完成真实链接解析而未执行，转由后续端到端导入验收覆盖。iOS 未验证。

`BUG-005` 已由项目负责人完成 Android 复测并进入 `DONE`：打开原 failed 任务后显示“结束此导入”，点击期间显示“正在结束…”且不可重复点击，最终持久化为“解析已取消”；首页未完成数量从 `6` 减少到 `5`，强制停止并重启后仍不再计入未完成。

`BUG-003` 本轮 Android 复测失败点从 `65%` LLM Schema 转移到公开内容获取阶段：新任务进入“正在获取公开内容”和“正在生成结构化菜谱”，离开原 `65%`，但失败文案为 `The public page did not expose usable text or media.`（尝试 1/3 · 不可重试）。已定位为 `PublicPageImportContentAdapter.fetch` 的“无文本且无媒体”判空分支；修复后首测进一步确认真实页面正文包含目标笔记 ID，解析器对真实页面结构提取结果为空。

已按 `ADR-0016` 实施修复：判空时先检测响应是否包含目标笔记 ID，包含则提示“已获取到该笔记页面，但暂时无法提取有效内容”（不可重试）；不包含则提示“未获取到目标笔记内容，链接可能已失效、需要登录或平台临时限制了访问”（可重试）；扩展登录墙、内容不可用、风控/限流标记并映射为稳定错误；错误文案本地化为中文可操作表述。`BUG-003` 返回 `VERIFY`，等待项目负责人 Android 复测新文案与阶段推进。

项目负责人回传的调试摘要确认根因：`finalPath=/404/sec_AkEqEBfZ`、`ogTitle=false`、`ogImage=false`、`ldjson=0`、`title=小红书`——真实请求被小红书安全机制重定向到 `/404/` 安全验证页（`sec_` 风控令牌），页面是验证/失效壳页而非目标笔记页，因此解析器提取不到任何文本或媒体。已新增 `_throwForSecurityOrNotFound`：`resolvedUri.path` 为 `/404` 或 `/404/...` 时映射为可重试 `networkUnavailable`，文案“平台未返回笔记内容（可能正在执行安全验证，或笔记已失效）…”，并在判空之前执行。调试摘要仅输出控制台/Logcat，不进入用户可见错误消息；等待项目负责人复测 `/404` 识别文案与平台放行情况。

`IMPORT-004` 待确认草稿重新生成已实现并进入 `VERIFY`：草稿确认页新增“重新生成”按钮（二次确认、busy 防重复），`AiRecipeBackendFacade.regenerateImportDraft` 复用会话原文证据重新执行 LLM Processor；成功时新草稿替换旧草稿（任务保持 `needsReview`），失败保留旧草稿，无会话证据（重启后）按钮禁用并提示“原始内容已过期，请重新导入”。等待项目负责人 Android 复测。

`IMPORT-005` 菜谱多封面图与详情页轮播已实现并进入 `VERIFY`：编辑页可从相册多选图片，`DeviceRecipeCoverImageStager` 复制到应用私有目录持久化；SQLite v4 新增 `recipe_images`；详情页顶部 PageView 轮播（无图回退像素插画）；首页/菜谱库卡片显示封面首图；编辑页可“设为封面”（移到首位）；修复了收藏切换清空封面的 Bug。等待项目负责人 Android 复测。导入图文自动带多图登记为 `IMPORT-006`（TODO），待导入链路稳定后实现。
根据 `ADR-0015`，Codex 不执行自动测试、静态分析命令、构建、模拟器、真机或真实平台/API 网络请求；只实施代码和编写 Android 测试方法。项目负责人执行后反馈，Codex 再归档验收和 AI 质量记录。服务器端、完整多协议矩阵和 iOS 继续暂缓。

## 当前主任务

`FRIDGE-002`/`RECO-001`/`FRIDGE-003`（DOING）：冰箱与按食材推荐首版实现。已按产品 5.8 / flows/FRIDGE_AND_RECOMMENDATION_FLOW / DESIGN-005（UI-014/015/016 原型）完成：①底部导航改四栏「首页/菜谱库/冰箱/我的」；②库存批次领域模型 + SQLite v5（inventory_batches/inventory_changes）+ CRUD/用完/丢弃/摘要/筛选 + 冰箱页轻拟物分区库存（冷藏/冷冻/常温/其他、聚合展开批次、五种状态徽标、空态）+ 批次新增/编辑页（存放位置跟随分区、数量为空=未知、同名提示）；③推荐页确定性离线匹配本地正式菜谱（已具备/缺1/缺2/优先清库存/更多缺失折叠、已选命中/冰箱已有·可补选/缺失/临期标记、数量是否足够需确认）；④烹饪完成自动进入扣减确认页（预计消耗、默认到期更早批次、逐项勾选/改量/换批次、确认才更新并记录 InventoryChange、不做负库存）。缺失项加入购物清单因购物清单模块未实现，本版仅展示缺失。**本轮按项目负责人《解决方案.md》升级为 ADR-0022 渐进式语义食材系统**：新增 `IngredientSpec`/规格词典/三值匹配（精瘦肉→猪肉=能做、猪肉→精瘦肉=可能能做、五花肉→精瘦肉=规格不符）；数据库 v6 落库规格字段；推荐页新增"可能能做"分组+"视为可用"会话确认+"规格不符"展示；库存录入页渐进式规格候选（可跳过）；菜谱保存时规格解析落库；草稿确认页"种类待确认"轻量标识。**继续按《解决方案.md》升级 ADR-0023 食材家族与模糊匹配（通用）**：新增 `IngredientFamily`（辣椒/菌菇/鱼/叶菜/肉/奶酪）与家族映射，同一家族无属性冲突→MAYBE（辣椒→青椒、蘑菇→香菇、鱼→鲈鱼、青菜→小白菜）、属性冲突→NO（干辣椒→青椒）；`IngredientImportance` 菜谱食材要求强度（菜名核心/可选不计缺失）；修复 AI 导入调料误计缺失（常备名单扩展）与库存规格未落库回退解析（`_batchEffectiveSpec`）。已清空模拟器菜谱数据（recipes 等表+缓存封面），保留库存批次、分类与设置。测试定义已新增（库存用例/推荐三值匹配/规格落库/家族匹配/可选不计缺失/FridgePage UI）。等待项目负责人构建 Android 按各验收文档复测。iOS 未验证。

**本轮追加（项目负责人反馈"库存食物不能删除，应点进去编辑时提供删除按钮"）**：`fridge_batch_edit_page.dart` 编辑态在「保存修改」下方新增红色「删除批次」按钮（key `deleteInventoryBatchButton`）——二次确认（`showPixelConfirm`，confirmColor 红）后调 `AiRecipeBackendFacade.deleteInventoryBatch(batch.id)`，成功后 `pop(true)` 返回库存页并刷新；新增态不显示删除按钮。测试：`fridge_page_test` 新增「新增态无删除按钮 / 编辑态有 / 取消确认保留 / 确认删除成功（清单为空）」。**再本轮追加（项目负责人反馈①冰箱/我的页右下角不要显示全局添加菜谱 FAB；②推荐页「仅使用已选食材」位置不合理）**：①`app_shell.dart` 全局 `floatingActionButton`（`addRecipeFab`）改为仅在首页/菜谱库显示（`_selectedIndex <= 1`），冰箱/我的不再显示；冰箱页自身「添加食材」FAB 保留。②`fridge_recommendation_view.dart` 把「仅使用已选食材」开关从底部操作栏移到顶部选择区（紧跟快捷操作之后），作为本次匹配的范围偏好，底部仅保留推荐按钮。**再微调（2026-08-07）**：开关独占一行、文字左对齐；底部推荐按钮改为**悬浮**——`build` 用 `Stack`（`Positioned.fill` 列表 + `Align.bottomCenter` 按钮），按钮条带纸张背景 + 顶部细线，列表 padding.bottom 96→130，结果/筛选视窗不再被挤压。全项目 IDE 诊断无错误；未运行测试/构建/真机（ADR-0015）；等待项目负责人构建 Android 复测。iOS 未验证。

**项目负责人复测反馈（2026-08-04）**：①首次复测「瘦肉/豆干」匹配不到"香干炒肉"——根因 AI 导入把水/花椒粉/小米辣等解析成食材导致缺 4+ 样被过滤 + 库存规格未落库匹配弱化，已修复（常备名单扩展 + `_batchEffectiveSpec` 回退解析）；②「辣椒」匹配不到「青椒」——根因家族判定未覆盖候选查找，已修复（`_candidateBatches` 纳入同家族）；③已确认「辣椒→青椒」可命中「可能可以做」。推荐卡片标签已优化：待确认食材不再误标"冰箱已有·可补选"。**第十六轮按项目负责人反馈：开启"仅使用已选食材"时替代（MAYBE）仍算命中**——strictSelected 分支排除 `verdict==maybe`，MAYBE 替代不因未选择而判缺失（走正常路径，进入"可能可以做"，显示仍为黄色"替代 xx→yy"），NO/YES 未选择仍按缺失计；补充测试定义。其余 RECO-001 复测项（精瘦肉→猪肉/猪肉→精瘦肉/视为可用/蘑菇→香菇/干辣椒→青椒等）待项目负责人继续反馈。

**UI-002 视觉 1:1 复刻（ADR-0024，进行中）**：项目负责人反馈当前界面"与原型类似但非 1:1"，要求按 HTML 原型复刻、原型有而实际没有的组件按原型风格补齐。第一轮主题级像素基建：阶梯缺角三档精确化、按钮/输入/FAB/Chip 像素缺角化、全局米白渐变+8px 圆点背景纹理、自绘等分像素底部导航、空状态 92px 浮动图标块、分区标题/虚线分隔。第二轮修「我的」页：inset 描边带消除"w"波浪、身份卡米白+52px 头像、能力配置改"能力配置"标题+平直 row-item、`PixelRowTile` 平直化+图标块 40px。第三轮对照 prototype.css 统一描边规范与切角：inset 描边带精确化（"切角外轮廓−内缩矩形"差集）、`--px-line`(line2 1.5px)/`--px-line-ink`(ink 2px) 对齐、`.ri-tag` 1px 描边、绿色系背景 greenSoft、`.notice` 去全描边、sec-title 补投影色块、背景圆点 1px、身份卡右上角绿色「登录/注册」按钮、HTML 缺角组件统一切角核查。**第四轮按项目负责人反馈修正**：①页面背景渐变→纯色 `#F3F1E9`（--paper，保留圆点纹理）；②设置条目（row-item）描边 `Border.all` 居中描边→inset 1.5px 描边带（`_InsetRectBorderPainter`，与 CSS `inset 0 0 0 1.5px` 一致）；③徽标描边改 inset 1px 带（切角处描边完整）；④按钮/FAB 墨色描边 2px→1.5px（项目负责人要求更细）；⑤`app_theme_test` scaffold 背景断言改 transparent。**第五轮按项目负责人反馈对齐设置页顶部 navbar**：`PixelPageAppBar` 返回按钮改原型 `.navbar .nb-btn` 样式（32×32 pxc-xs 切角 + card 底 + inset 1.5px line2 描边 + 左箭头，保留"返回"Tooltip）、背景 card 94% 半透明、底部实线改 2px 虚线、标题 15px/w800/letter-spacing 1px、高度 58→52；LLM/OCR/隐私三个设置页移除 eyebrow（原型 navbar 只有标题）。**第六轮按项目负责人指定改整行条目立体描边**：`PixelRowTile` 外框由浅灰 inset 带（#CFC9B6）改为像素风立体边框（左/上亮边 `#545454`、右/下暗边 `#000`，宽 1.5px，`_BevelBorderPainter`），影响全部 row-item；chip 选中态描边补 green-deep 1.5px（HTML `.chip.active`）。**第七轮按项目负责人要求复刻冰箱页（UI-014）+ 分区可折叠**：①顶部大标题→`.seg` 二级切换（PixelSeg）；②页头「我的冰箱」+ tiny 副标题 + `.ri-tag.ok` 绿色去推荐按钮；③`.stat-grid.cols4` 摘要（PixelStatCard）；④筛选 chip 平直化（选中绿底+深绿描边+白字）；⑤冰箱箱体（#EBEFEA 底 + pxc-lg 切角 + 2px inset 描边 + 门缝虚线）；⑥分区卡 `_ZoneCard` 改可折叠（details 行为、zone-ic 26px、zone-add、agg-arrow 旋转 90°、zone-body 顶部 1.5px 分隔、冷冻区浅蓝灰底 + 抽屉把手、空分区虚线框）；⑦`_FoodChip` food-chip 样式（底部 3px 硬投影 + inset 1.5px 描边 + 状态变体 + 食材图标 + `.conf` 标签）；⑧筛选联动隐藏无匹配分区；⑨空状态 92px 图标块；⑩背景透明露出全局 #F3F1E9。新增共享组件 PixelSeg/PixelStatCard/PixelConfTag/PixelIconBtn，PixelSurface 增加 borderWidth。**第八轮按项目负责人反馈补冰箱状态标签 + 长按拖动排序/跨区移动**：①批次展开行补 `.conf` 状态标签（`_conditionTag`）；②长按拖动全链路——`InventoryBatch`/`InventoryBatchInput` 加 `sortOrder`，DB v7 迁移 `sort_order` 列（幂等），仓库按 `sort_order, created_at` 排序，用例/Facade 新增 `moveBatchToZone`/`reorderBatch`（记 adjust 变更），UI 用 `LongPressDraggable`+`DragTarget`（chip 级 + 分区级）实现区内排序（拖到目标 chip 前/空白→末尾）与跨区移动，落点后本地重排 + 持久化，失败回滚重载。**第九轮按项目负责人复测修复冰箱三问题**：①跨区拖动无效——根因嵌套 DragTarget 双回调并发冲突，改为 chip 级精确插入（按名称）+ 分区级兜底（空白→末尾）+ 250ms 去重，空分区可接收拖入；②设置了日期仍显示"未设置日期"——`_aggregateCondition` 未记录 `normal`，补 `hasNormal` 后正常/临期/过期/未设置与 HTML 一致；③拖动触发全量刷新+加载动画——移除 `_applyDrop` 的 `onDataChanged`，本地即时更新，失败才回滚重载；`loadAggregated` 排序改按分区+`sortOrder`（保持拖拽顺序）。**第十轮按项目负责人反馈**：①修复日期保存与"有些不能设置"——单位/分类下拉框当前值不在预设列表时补入选项（避免 value 不在 items 断言崩溃），`showDatePicker` 改用默认 Material 主题 builder 渲染面板（像素主题可能干扰系统日期组件）+ 异常兜底；②冷冻区冰冻视觉+动画——`_FoodChip` 加 `frozen`（浅冰蓝底 #E8EFF3 + 蓝灰描边/投影 #9AB0BE + 冰蓝图标，conf 标签保留）；③`PixelConfTag` 在切角表面下加 1px inset 内描边（前景色 45% alpha）。**第十一轮按项目负责人反馈修复**：①日期保存后不立即同步——根因 `_InventoryView` 依赖 AppShell 的 `refreshToken` 链路，批次编辑保存返回后未触发；`_FridgePageState` 新增 `_localRefresh` 本地计数（编辑保存/批次操作成功后递增并传给 `_InventoryView` 的 `refreshToken`），保存后立即重载显示新日期；②冰冻/解冻动画未生效——根因 AnimatedSwitcher 的 350ms 淡入淡出在跨区拖动后 chip 被重新构建（新 State 直接落在最终态，无动画起点）；改为 `_FoodChip` 自维护 450ms `AnimationController` + 新增 `_FrostFillPainter` 逐格绘制器（8px 一格，按 `floor(progress×cols)` 从左往右逐格切换源/目标色），同一位置内 frozen 变化由 `didUpdateWidget` 捕获播放，跨区拖动产生的新 chip 通过 `_pendingFrostAnim` 标记（`_applyDrop` 记录名称+方向）在 `initState` 播放——放入冷冻区"正常→冰冻"、移出"冰冻→解冻"，描边/投影/图标同步 `Color.lerp` 过渡。**第十二轮按项目负责人复测修复**：①非冷冻区误显冰冻样式——根因 `_FrostFillPainter` 在 `freezing=false`+`progress=0` 时源色=frozenColor 整块画冰蓝，静止态非冷冻 chip 的 `_controller.value` 落成 0；修复静止态进度恒为 1（动画已完成=目标色全填充），动画只在 `playFrostOnMount`/`didUpdateWidget` 触发时 `forward(from: 0)` 播放，仅冷冻区为冰蓝；②逐格动画阶梯感——格子 8px→16px、时长 450ms→900ms，逐格跳变清晰（非平滑扫过）；③日期同步补漏——第十一轮 `_localRefresh` 虽触发重载但 `_allItems ??=` 沿用旧缓存导致仍显示旧数据，`didUpdateWidget` 在 refreshToken 变化时先置 `_allItems = null` 再加载。**第十三轮按项目负责人《解决方案.md》+ `FRIDGE_DRAG_AND_FROST_ANIMATION_HANDOFF.md` 重构拖拽与冷冻动画**：①动画触发与手势彻底分离——新增 `_FrostKind`/`_ArrivalEvent`（dragSessionId/moveEventId/stableItemId/animationKind），到达事件只在“最终接收+保存成功+目标挂载”后生成一次、消费即删除，删除名称+250ms 去重与 `_pendingFrostAnim`；②每分区单一最终 DragTarget（onMove 算插入序号、onLeave 清高亮、onAccept 提交），chip 不再作提交目标，`_applyDrop` 加会话级一次性提交守卫；③`AggregatedInventoryItem` 新增 `stableId`（批次 ID 集合）稳定身份，同名多批次/快速连续/重建不串事件；④组件拆分——静态 `_FoodChipView`（无 controller/无挂载播放/无水波纹）用于拖拽影子与占位，正式 `_FoodChip` 只在消费到达事件后播放；⑤二维逐格动画 `_FrostGridPainter`——12dp 二维小格（≥2 行）、图标侧垂直居中冰核、曼哈顿距离由近到远、每 tick 只切一个完整格子（冷冻逐个出现/解冻严格逆序消失）、550ms、描边/投影/图标离散切换（不再 Color.lerp）；⑥移动规则——同区排序/非冷冻互移/长按/无效释放均不播动画，仅跨冷冻边界保存成功挂载后播一次，保存失败恢复原位+中文提示；⑦减少动态（`MediaQuery.disableAnimations`）直接显示最终样式。**第十四轮按项目负责人反馈修推荐页食材样式（对齐 HTML）+ 待确认改黄色替代标签**：①选择区食材 chip 由圆角卡片改为 HTML .food-chip（平直 + 底部 3px 硬投影 + inset 1.5px 描边 + 图标 + 名称/数量 + 状态变体，选中态 green-soft 底 + green-deep 描边 + 名称后 ✓，已过期 0.7 透明度，关闭水波纹）；②快捷按钮改 .chip 平直样式；③推荐卡片食材标签重构为 HTML .mini-tag（已选命中=绿、冰箱已有可补选=蓝、缺失=琥珀且带数量“缺失 · 鸡腿 400 g”、规格不符=红）；④待确认（MAYBE）改黄色“替代 牛肉→猪肉”标签——`IngredientMatchDetail` 新增 `matchedStockName`（`_BestMatch` 记录命中库存食材名），移除原“待确认：…— 原因”文本行，仅保留“视为可用”按钮。**第十五轮按项目负责人反馈修推荐卡**：①缺失标签由琥珀改灰色（`_tagMiss*` 灰系，替代标签独立为黄色 `_tagSub*`）；②移除“视为可用”按钮（删 MAYBE 确认块及 `onConfirmMaybe` 参数链、`_confirmBaseForSession`，`confirmedBases` 字段保留恒空）；③移除“有该食材，数量是否足够需确认”文本；④推荐卡头部改 HTML .card 结构——左侧 56×56 封面（有 `coverImage` 显示本地图片、无图回退绿块+菜名图标）+ 标题/tiny + 右侧匹配 conf，新增 `_cardCover`/`_coverIcon`。**第十七轮按项目负责人反馈修首页快速导入**：①快速导入区去 2×2 网格与外层绿底卡，改 HTML .quick-grid 一行 4 个 quick-btn（竖排 36×36 green-softer 图标块 pxc-xs 切角 + 文字，card 底 + pxc-sm 切角 + 墨色 1.5px inset 描边同 FAB 添加按钮）；②剪贴板改文本输入导入——点“剪贴板”弹文本输入框，确认后 `createLocalTextImportTask` + `runImportWithText`（允许 queued，自有 LLM 整理成菜谱），`ImportProgressPage` 加 `initialText`/`_runTextFallback`，不再读系统剪贴板预填链接。**第十八轮剪贴板弹窗改像素风**（`_promptPasteText` 由 AlertDialog 改为透明 Dialog + PixelSurface 容器：pxc-lg 切角 + 墨色 1.5px 描边 + 硬投影，标题 w900 + tiny 副标题 + 像素输入框 + 像素按钮；随后修复弹窗取消报 overlay 断言——controller 改由独立 `_PasteTextDialog` StatefulWidget 持有随生命周期释放，并补 `app_theme` import）。**第十九轮菜谱详情**：来源链接点击复制到剪贴板（`Clipboard.setData` + 浮动 SnackBar），封面图点击打开像素风全屏查看器 `_FullscreenImageGallery`（深色背景 + PageView 左右滑动切换 + InteractiveViewer 双指缩放 + 像素关闭按钮 + monospace 页码）。全项目 IDE 诊断无错误，未运行测试/构建/真机（ADR-0015）。下一步：项目负责人复测①首页快速导入一行+描边；②剪贴板文本输入导入与像素弹窗；③来源链接复制；④图片放大左右滑动。

`IMPORT-009`（DOING）：导入体验补强。项目负责人反馈 4 点，代码与测试定义已完成：①进度条填充高度为 0（`PixelProgressBar` 的 `FractionallySizedBox` 缺 `heightFactor`，子项无固有尺寸导致高度 0）已修；②正文与简介重复（WebView 提取 description==bodyText）已去重；③快速导入 4 按钮分流（粘贴链接→添加页、剪贴板→读系统剪贴板预填、拍照选图→本地 OCR 图片导入新任务、手动创建→直接打开编辑器）；④链接批量导入（多链接一次创建多个任务，≥2 个成功后自动进入任务列表）+ 新增未完成导入列表页（排队/解析中/待确认/失败/已取消，单个取消/删除、一键清空已结束，二次确认），首页"有 N 个未完成导入"入口改打开列表；Facade 新增 `deleteImportTask(s)`/`createLocalImageImportTask`，`runImportWithLocalImage` 放宽允许 queued。**2026-08-06 按用户反馈新增未完成导入长按多选删除与全选**：长按任意任务进入选择模式并选中该项、选择模式下点按切换选中（全部取消自动退出）、顶部"已选择 N 项"+"全选/取消全选"+"取消"、底部浮出"删除所选 (N)"批量操作栏（二次确认，复用 Facade `deleteImportTasks`）；**用户追加需求：进行中/待确认任务也可删除，弹窗提示**——`deleteImportTask` 对进行中/待确认任务先走取消流程（中断后台解析）、再清理全部关联草稿（主草稿 + 附加草稿 `allResultRecipeIds`，防孤儿草稿）、最后永久删除；确认弹窗提示"其中 X 条正在进行或待确认：删除将中断解析并移除已生成的 AI 草稿"，底部操作栏提示"进行中/待确认任务将一并终止"；选择模式下隐藏单个取消/删除按钮，选中项深绿描边 + 勾选标记，底部预留空间避免操作栏遮挡；新增 7 项 Widget 测试定义。**2026-08-06 用户反馈"弹窗风格没统一"，已完成全项目弹窗统一**：新建共享像素弹窗组件 `pixel_dialogs.dart`（`PixelConfirmDialog`/`PixelInfoDialog` + `showPixelConfirm`/`showPixelInfo`），清除全部原生 `AlertDialog`（12 处）、删除三个本地重复确认类（`_PixelConfirmDialog`/`_LibraryConfirmDialog`/`_DevConfirmDialog`）、`recipe_edit_page` 内联"未保存修改"弹窗对齐基准样式；特殊弹窗（多草稿选择/生成进度/分类输入/剪贴板输入）本身已是像素容器保留。全项目 IDE 诊断无错误。等待项目负责人构建 Android 按 `tests/acceptance/IMPORT-009-import-experience-2026-08-03.md` 复测（含弹窗观感与交互）。iOS 未验证。

`IMPORT-008`（DOING）：链接导入改用内置浏览器内核（WebView）抓取网页文本与图片，通用任意网页。Android 首版实现后真机暴露两类独立故障：①"网页加载超时"（单一 45s 超时掩盖真实阶段）；②配图 403（客户端直连被小红书 CDN 拒绝）。已按高级工程师方向（ADR-0019）完成重构：WebView 抓取改为显式阶段状态机 `NAVIGATING(≤18s) → WAITING_CONTENT(≤12s，DOM 探测) → EXTRACTING(≤5s) → FETCHING_IMAGES(总≤25s/单张≤9s)`；不再依赖 `onPageFinished`（主框架提交 `onPageCommitVisible` + 轻量探测）；图片改为逐张顶层导航取图（图片文档同源 `fetch(location.href)` 读原始字节，Canvas 导出兜底，最多 9 张、逐张写盘、失败继续下一张）；正文成功时图片失败只产生"配图部分失败"，不整体失败；WebView 改默认移动 UA + 360×800 视口，移除对第三方页面的 addJavascriptInterface；Dart 侧小红书/抖音不再回退远程直连（避免重复 403 混淆日志），web 平台保留直连快速路径。**顶层图片同源读取可行性依赖 Android WebView 具体实现，已立 SPK-007 真机 Spike 前置验证（未验证前不视为已解决）**；等待项目负责人 `flutter run` 真机按 `tests/acceptance/IMPORT-008-webview-import-2026-08-03.md` 分阶段核对日志（extracted/每张图 via fetch|canvas/失败原因）复测。iOS（WKWebView）暂缓。

**已合入《解决方案.md》第一阶段竞态与生命周期加固（ADR-0032）**：①`RequestContext` 请求隔离——每次抓取独立状态，所有异步回调统一校验 `ctx === activeRequest && !ctx.cancelled`，且 `navigationGeneration`（每次主框架导航递增）/`imageGeneration`（每张图片递增）双代次使旧导航/旧图片迟到回调一律丢弃；②新请求 `superseded` 正确终止旧请求（取消计时器+stopLoading+清理目录+完成旧 result，不显示超时）；③通道新增 `cancelFetch`，Dart `WebViewContentFetcher.cancel()` 由导入取消令牌触发，原生立即停止导航/JS/图片下载写盘，用户取消记为 `cancelled` 而非超时；④以 Dart timeoutMs 建立请求级绝对 `requestDeadlineMs`，`armDeadline` 取 `min(请求剩余,阶段,单图)` 调度，阶段不再能累计超过总预算；⑤`onRenderProcessGone` 后 `destroyWebView()` 置空、下一次请求重建，错误分类 `rendererGone`；⑥图片写入请求专属目录 `webview-imports/<requestId>/images/`。Dart 侧新增 `cancelled/superseded/rendererGone` 错误分类。等待项目负责人按 `tests/acceptance/IMPORT-008-webview-lifecycle-race-2026-08-06.md` 复测。

**真机定位并修复（2026-08-06）**：小红书链接在进入 EXTRACTING 后，SPA 客户端路由
`history.pushState` 跳到相对路径 `item/xxx`，触发主框架 `onReceivedError
ERROR_UNSUPPORTED_SCHEME(-10)`，旧逻辑把所有主框架错误当致命错误 → `abort:
页面加载失败（-10）`，尽管正文已就绪。修复：①`onReceivedError`（新旧两版本）仅在
**NAVIGATING** 阶段对主框架错误中止，WAITING_CONTENT/EXTRACTING 阶段的 SPA 路由
错误视为噪音忽略，由提取脚本判定内容；②`onPageStarted` 仅在 NAVIGATING 阶段递增
`navigationGeneration`，避免 SPA 客户端路由作废已发出的探测/提取回调；③
`onReceivedHttpError` 在 WAITING_CONTENT 阶段对主框架 404/410 立即 `emptyContent`，
其他状态交给探测/提取。**注意：需重新构建 Android（Kotlin 原生改动）后复测。**

**已合入《解决方案.md》第二/三阶段（ADR-0033）**：①平台 readiness 分流 `probeScript(platform,targetId)`——小红书/抖音/通用三套探测（目标 ID 一致性、非登录/验证/删除/受限、主正文/JSON-LD/正文达标），`probeContent` 由"有文字即稳定"改为按 ready 决策；②平台特化提取——抖音 `__RENDER_DATA__`（videoInfoRes/awemeDetail/aweme_list）标题/描述/作者/封面，通用 JSON-LD Recipe/Article 优先 + 主正文回退，小红书补输出 fingerprint，`validateResult` 把登录/验证/删除归为 `loginWall/verification/gone`；③正文与图片彻底解耦——图片阶段 `onReceivedSslError`/`onRenderProcessGone` 只结束配图抓取不整单失败；④可观测性——`RequestContext` 记录 redirectCount/finalHostPathHash/httpStatusCode/readinessReason/bodyFingerprint/bodyLength/图片统计/rendererGoneCount/stageStartedAt/finalErrorClassification，请求结束输出一行 `OBS req=... stage=..ms redirect=.. http=.. readiness=.. fp=.. imgAttempt/Ok/Fail=.. final=.. totalMs=..` 汇总日志。等待项目负责人按 `tests/acceptance/IMPORT-008-webview-readiness-observability-2026-08-06.md` 复测（小红书/抖音/通用 readiness、JSON-LD、登录墙/删除分类、图片阶段 SSL/renderer gone 不整单失败、OBS 字段）。

`IMPORT-006`：链接导入自动获取配图作为封面。`LlmRecipeGenerationProcessor` 生成草稿时把公开内容配图经 OCR-002 安全暂存下载到 `recipe_covers/<recipeId>/` 并写入草稿 `images`/`coverImage`；单张失败 best-effort、全部失败草稿仍生成；丢弃草稿同步清理封面目录。真机 403 根因（IPv6 节点）已修复为 IPv4 优先 + 地址级故障转移，`cover_image` 残留远程 URL 与残缺图片 URL 已修复；图片来源将随 `IMPORT-008` 改为浏览器会话获取后一并复测。等待项目负责人按 `tests/acceptance/IMPORT-006-remote-covers-2026-08-03.md` Android 复测。

`IMPORT-007`：链接导入启用本地 OCR。`runImportTask` / `dispatchPendingImports` 执行计划自动解析：本地 OCR 能力就绪时启用 `ocr: local`，图文内容把图片识别成文字与正文整合后统一交给 AI；OCR 未安装时回退纯文本路径（有正文仍可生成，纯图片提示安装）。OCR 链路错误文案已中文化。等待项目负责人按 `tests/acceptance/IMPORT-007-link-import-ocr-2026-08-03.md` Android 复测。

`BUG-003`：公开内容 Adapter 已按 `ADR-0016` 完成“无可用内容”分类修复：判空时按响应是否包含目标笔记 ID 给出不同中文可操作错误与可重试语义，并扩展登录墙、内容不可用、风控/限流标记。规范化公开样本为 `https://www.xiaohongshu.com/explore/6a421c6b00000000170297f9`；仓库不保存原始查询参数、访问令牌、Cookie、Authorization、API Key、完整页面正文、完整 Prompt 或完整模型响应。

`BUG-005`：Android 验收通过并已关闭；后续仅在回归发现退化时重新打开。

当前事实边界：旧 failed 任务收尾已通过真实 Android 验收；新小红书任务在公开内容可用性判定处失败，本轮已实施 Adapter 分类修复并进入 `VERIFY`。下一步由项目负责人安装新版本，使用带完整分享参数的链接或规范化公开样本复测：应看到中文可操作错误（未返回目标内容时提示检查链接/稍后重试/降级，并显示可重试）；若页面实际返回目标详情仍无法解析，再反馈真实截图继续定位。仓库继续不保存完整页面正文、敏感查询参数、Prompt 或模型响应。

`BUG-002`、`AI-003` 与 `SPK-003` 保持 `VERIFY`；其 Android 验证状态继续按各自验收记录处理。临时 API 地址与 Key 不得写入仓库、日志或验收文档。

`UI-001` 保持 `VERIFY`：Flutter 原生高保真复刻和既有 Android 截图矩阵已完成；页面实现仍以 `design/prototypes/` 和 `docs/architecture/FLUTTER_BUSINESS_BACKEND_COMPLETION.md` 为准，不得绕过 `AiRecipeBackendFacade` 直接访问 SQLite、SharedPreferences、安全存储、Repository 或 Provider。
## 已完成基线
- `APP-004` 已完成：标签与搜索、首页/详情聚合、最近浏览、烹饪会话与多计时器、本地隐私/历史设置、LLM 设置和统一 Facade 已补齐。
- 菜谱与分类 CRUD、收藏、搜索、筛选、复制和回收站生命周期。
- 游客/登录会话元数据和能力矩阵。
- 公开链接导入任务、取消、重试、恢复和调度。
- 小红书/抖音公开内容 Adapter 与人工降级输入。
- OpenAI-compatible、Gemini Provider，严格结构化菜谱生成和草稿确认。
- OCR/ASR Provider 契约、OCR 模型包管理和 Android 首阶段 OCR Runtime。
- `OCR-002` 远程 HTTPS 图片安全暂存层。
- SQLite Schema v3 及 v1/v2 → v3 数据迁移。
- 2026-07-30 APP-004 质量门：`dart analyze`、`flutter analyze --no-pub` 均无问题，`flutter test --no-pub --concurrency=4` 共 356 项全部通过。
- `UI-001` 首个切片已完成：应用主题、组合根生命周期、欢迎/游客入口、四 Tab 应用壳、首页、菜谱库、链接/手动添加入口、我的页和菜谱详情快速预览均已接入 `AiRecipeBackendFacade`。
- 2026-07-30 UI 首切片质量门：`flutter analyze --no-pub` 无问题，`flutter test --no-pub --concurrency=4` 共 364 项全部通过；新增 8 项主题与 Widget 测试。
- `UI-001` 菜谱详情/编辑/回收站切片已完成：正式详情页、完整新建/编辑表单、软删除、恢复、永久删除和清空回收站均通过 Facade 接线；新建、编辑、删除和恢复后会刷新首页与菜谱库。
- 2026-07-30 UI 第二切片质量门：3 个定向测试文件共 7 项通过；`dart analyze code/apps/mobile/lib code/apps/mobile/test` 与 `flutter analyze --no-pub` 均无问题；`flutter test --no-pub --concurrency=4` 共 371 项全部通过；`git diff --check` 通过。
- `UI-001` 导入执行进度/AI 草稿确认切片已完成：创建任务后自动执行导入流水线，覆盖轮询、取消、重试、恢复、失败/取消/完成状态；AI 草稿支持低置信度提示、编辑食材与步骤、保存发布、放弃草稿和会话级证据查看。
- 2026-07-30 UI 第三切片质量门：2 个定向测试文件共 6 项通过；`dart analyze lib test` 与 `flutter analyze --no-pub` 均无问题；`flutter test --no-pub --concurrency=4` 共 377 项全部通过。
- `UI-001` 完整烹饪模式切片已完成：详情页可启动或恢复烹饪会话，支持当前步骤恢复、上一步/下一步、完成烹饪、多计时器创建/暂停/继续/提前完成/清除、防误触退出和前台归零提醒；页面只通过 `AiRecipeBackendFacade` 接线。
- 烹饪计时器以持久化的绝对 `endsAt` 为剩余时间事实源，页面 ticker 只刷新显示；退出页面后活动会话和运行计时器继续保留。当前尚未接入 App 退出后的操作系统后台本地通知。
- 当前 `RecipeStep` 尚无独立食材关联字段，页面只展示步骤正文中完整提到的食材；`CookingTimer` 尚无步骤索引字段，当前以稳定标签 `步骤 <序号>` 关联步骤，不伪造不存在的数据关系。
- 2026-07-30 UI 第四切片质量门：烹饪模式与详情页 2 个定向测试文件共 8 项通过；`flutter analyze --no-pub` 无问题；`flutter test --no-pub --concurrency=4` 共 383 项全部通过。
- `UI-001` 设置与 onboarding 切片已完成：onboarding 与身份会话独立持久化，首次游客、已确认游客和已登录用户走不同入口；Profile 已接通 LLM、OCR、隐私三个真实设置页。
- LLM 设置只通过 Facade 读取、保存、测试和清除密钥，支持 OpenAI-compatible 与 Gemini；API Key 不明文回填，错误状态不包含输入密钥或服务端敏感原文。
- OCR 设置只展示真实模型状态；正式 App 未配置生产 Manifest 时禁用安装，不伪造下载地址、哈希、大小或可用状态；托管云 OCR 仍按登录与上传权限边界保持不可用。
- 本地隐私设置支持浏览历史、文本/图片/视频上传授权和历史清理；关闭历史会立即清空。
- 2026-07-30 UI 第五切片质量门：7 个定向测试文件共 24 项通过；全量回归首次发现 onboarding 偏好实例在组合根构造期提前要求插件平台，已改为首次读写时延迟创建；修复后 `flutter analyze --no-pub` 无问题，`flutter test --no-pub --concurrency=4` 共 404 项全部通过，`git diff --check` 通过。
- `UI-001` 导入失败人工降级切片已完成：失败/取消任务支持粘贴正文、截图、视频和手动创建四个入口；粘贴正文跳过公开 Adapter、保留原任务来源并复用统一 Processor，非法状态、空正文和 Provider 不可用均使用稳定脱敏错误。
- 添加页链接入口已补接线测试：小红书链接会创建可持久化 queued 任务并交给进度页，空链接不会创建任务。
- 2026-07-31 UI 第六切片质量门：核心 5 个定向测试文件共 52 项通过，添加页 2 项通过；`flutter analyze --no-pub` 无问题，`flutter test --no-pub --concurrency=4` 共 425 项全部通过。真实 Provider 质量验收因缺少用户授权 API 地址、Key 和公开样本而未执行。
- 2026-07-31 Android API 36 模拟器冒烟通过：Debug APK 构建、安装和启动成功；游客入口、四 Tab、手动新建、详情、收藏、软删除、回收站恢复、LLM/OCR/隐私设置与设置持久化主链完成，`integration_test/android_smoke_test.dart` 1 项通过。
- Android 冒烟期间修复菜谱编辑器分类加载乱码、集成测试未正确清理 SharedPreferencesAsync/DataStore 状态，以及首页/菜谱库刷新时从 `setState` 回调返回 Future 的运行时断言；刷新回归测试 2 项通过。
- 2026-07-31 Android 验收质量门：Logcat 对 `FATAL EXCEPTION`、`AndroidRuntime`、`E/flutter` 与 Flutter error 模式均无匹配；`flutter analyze --no-pub` 无问题，`flutter test --no-pub --concurrency=4` 共 427 项全部通过，`git diff --check` 通过。
- 本轮按用户要求只测试 Android；iOS 未验证且不阻塞本次 Android 冒烟结论。该冒烟不代表真实公开平台解析或真实 AI 输出质量。
- `UI-001` Android 单张截图选择切片已完成：失败/取消任务可打开系统 Photo Picker，取消后任务状态保持不变；图片选择、MIME 校验、lost-data 恢复和页面错误边界均已通过自动测试。
- Android API 36 人工验收已验证系统 Photo Picker、取消返回，以及选择图片后返回应用；当前设备未配置自有 LLM，能力检查先显示“请先在 LLM 设置中配置可用的自有 AI 服务”，未把该结果误写为 OCR 未安装设备验证。
- 2026-08-01 截图切片质量门：3 个定向测试文件共 43 项通过，Facade 单文件 19 项通过；`flutter analyze --no-pub` 无问题；`flutter test --no-pub --concurrency=4` 共 447 项全部通过。Logcat 扫描 1333 行，无崩溃、未处理异常或应用敏感路径匹配。
- Android Debug APK 全量构建首次因 `gradle.properties` UTF-8 BOM 导致 `org.gradle.jvmargs` 未生效、Daemon 仅使用 `-Xmx512m` 而 OOM；移除 BOM 并停止旧 Daemon 后构建成功。真实 PP-OCRv5、真实自有 LLM 和 AI 质量仍未验证。
- `UI-001` Flutter 原生高保真复刻 Android 验收完成：欢迎、首页、我的、LLM、OCR、隐私、回收站、添加、导入进度、导入失败、普通/低置信草稿、菜谱库、详情、编辑和烹饪共 16 张截图已归档到 `tests/acceptance/artifacts/UI-001-2026-08-01/`。
- 2026-08-01 UI 最终质量门：`flutter analyze --no-pub` 无问题；`flutter test --no-pub --concurrency=4` 共 450 项全部通过；Flutter 源码与 `pubspec.yaml` 未发现 WebView 引用。正式验收记录为 `tests/acceptance/UI-001-flutter-native-prototype-replication-2026-08-01.md`。
- `BUG-002` 业务后端加固完成：ImportTask CAS、写冲突重读、取消优先、迟到草稿安全补偿与 Facade 接线已通过 SQLite 真实文件竞态验证。
- `AI-003` / `SPK-003` OpenAI-compatible 首阶段验证完成：真实纯文本生成进入 `needsReview`，SQLite 重开恢复成功；临时 Key 已通过 App 内操作清除。
- 2026-08-01 后端质量门：相关定向测试 53 项全部通过；`flutter analyze --no-pub` 无问题；`flutter test --no-pub --concurrency=4` 共 475 项全部通过。
- `BUG-002` 页面取消竞态修复完成：页面拒绝低版本快照和取消后的非取消快照，迟到 Runner 证据不会与页面状态错配，宿主刷新异常不会覆盖持久化结果。
- 小红书 PC webshare 公开内容 Adapter 兼容切片已完成：支持 `window.__INITIAL_STATE__`、嵌套 `noteData`、裸 `undefined`、多图片字段、移动端请求头、查询参数顺序保留及通用站点标题过滤；最新 Android 反馈已确认真实请求能够进入公开内容获取与 LLM 生成。
- 2026-08-02 导入缺陷质量门：页面解析/Adapter/进度页联合定向 37 项、取消后端定向 53 项、全量 480 项均通过；`flutter analyze --no-pub` 无问题；相关 6 个 Dart 文件格式检查无变化。

## 本阶段明确暂缓

- `code/services/backend/` 的服务器实现。
- 真实认证协议、云同步和游客数据合并。
- 平台托管 AI/OCR/ASR、配额和云端队列。
- 冰箱与按食材推荐已完成 `FRIDGE-001` 规划，但 `FRIDGE-002`、`RECO-001`、`FRIDGE-003`、`RECO-002`、购物清单实现、数据导出/备份、周菜单和营养统计仍暂缓，未开始业务代码。
- `SPK-002` 的真实 PP-OCRv5 模型真机质量、完整 DB 后处理、方向分类器和 iOS Runtime。
- `SPK-003` 完整 Gemini 与局域网兼容服务矩阵；当前只执行已授权的 OpenAI-compatible 纯文本服务首阶段验证。

## 下一步

- `BRAND-002`：项目负责人重新构建 Android（`flutter run` 或 `flutter build apk --debug` + `adb install -r`）后确认桌面/应用管理器的应用图标已变为「巴食」图标（`design/assets/图标.png` 的裁剪版本）。确认后归档 BRAND-002 进入 `DONE`。
- `BRAND-001`：项目负责人重新构建 Android（`flutter run` 或 `flutter build apk --debug` + `adb install -r`）后确认：①桌面 / 应用管理器中的应用名显示为「巴食」（不再显示 ai_recipe）；②欢迎页品牌标题显示「巴 食」（字距与风格不变）；③「我的 → 关于」等页面无「拾味」残留。确认后归档 BRAND-001 进入 `DONE`。
- `BACKUP-002`/`BACKUP-003`：项目负责人构建并安装当前 Android Debug APK，按 `tests/acceptance/BACKUP-002-full-backup-export-2026-08-06.md` 验收：①「我的 → 数据与存储 → 创建全量备份」入口、预估/空/加载/错误态；②创建备份进度阶段与成功摘要；③用 `unzip -l` 核对 `.airecipe-backup` 内部结构（manifest/NDJSON datasets/media index/`media/sha256/` 去重与相对路径）；④取消与媒体缺失失败后 `backups/` 无 `.partial` 残留；⑤空库备份与两次创建幂等；⑥（可选）运行 `flutter test test/domain/backup_manifest_records_test.dart test/application/backup_export_integration_test.dart`。回传解压目录树与 `ls backups/` 输出。通过后 Codex 归档 BACKUP-002/003。
- `BACKUP-004`/`BACKUP-005`：在 `C:\AIM` 下执行 `flutter run`（或 `flutter build apk --debug` + `adb install -r`）安装后，按 `tests/acceptance/BACKUP-004-backup-import-2026-08-07.md` 验收：①「数据与存储 → 从备份导入」选择文件（默认/取消/非备份扩展名提示）；②损坏备份/未来版本/恶意路径构造稳定中文失败且不残留 staging；③计划展示统计（新增/跳过/重复/冲突）与「仅名称相似不自动合并」；④合并导入三策略（保留当前/用备份覆盖/两者保留）结果正确、原有数据不丢、重复导入幂等；⑤替换恢复（替换范围预览 + 二次确认 + 回滚备份 + 清库写入 + 孤儿清理 + 不影响账号/设置/冰箱）；⑥取消与 staging 清理账本。前置：先用「创建全量备份」产出一份 A 备份，再修改/新增/删除菜谱使当前库与其不同。回传各步截图与回滚备份路径。通过后 Codex 归档 BACKUP-004/005，再进入 BACKUP-006（跨平台与规模）。

> 新增规划走查：项目负责人先按 `tests/acceptance/FRIDGE-001-fridge-recommendation-planning-2026-08-03.md` 核对四栏导航、冰箱分区 UI、库存字段、选择食材推荐、缺 1/缺 2 分组与扣减确认规则；确认前 `FRIDGE-001` 保持 `VERIFY`，后续实施任务保持 `TODO`。

1. 复测 `IMPORT-010`（本轮新增）：按 `tests/acceptance/IMPORT-010-progress-detail-2026-08-06.md` 验证解析中实时详情——单图/多图"正在识别第 x/N 张图片"、分组后"识别为 N 份独立菜谱，正在生成草稿"、并发生成"正在生成第 x/N 份菜谱草稿/第 x/N 份已生成/全部草稿已生成"、单草稿"草稿《菜名》已生成，正在保存"，以及取消/失败/重试后无残留详情，并回传截图/录屏与 `[AIRecipe][Recognize]`/`[AIRecipe][Grouping]`/`[AIRecipe][Generate]` 日志。

1. 项目负责人安装包含 `BUG-003` Adapter 修复、`/404` 安全页识别、`IMPORT-004` 重新生成、`IMPORT-005` 多封面图、`IMPORT-006` 链接导入自动带图、`IMPORT-007` 本地 OCR 图文整合与界面文案全中文化的 Android 版本，一并查看首页/我的页等页面中文文案与游客徽标样式。
2. 在 App 的 LLM 设置页确认运行时配置仍可连接；在“我的 → OCR 引擎”确认本地 OCR 模型安装状态；聊天和仓库中不回传或保存真实 Key、API 地址、Authorization、完整 Prompt 或模型响应。
3. 复测 `BUG-006`（本轮实施完成）：在“识图引擎 → 多模态 LLM 图片识别”中验证三入口（检查基础连接 / 检查图片能力标准 256×256 图 / 用我的图片测试）与分阶段结果卡：确认不再出现笼统“链接超时”，能区分连接失败、无首字节、正文卡住、HTTP 408/504、图片协议不兼容、Schema 错误与图片断言失败；视觉专用模型拒绝纯文本时显示“服务与鉴权已通过，文本能力未确认”；取消不显示超时；自动模式（本地 OCR 与多模态同时就绪）走融合，OCR 成功/多模态失败时保留 OCR 并标记“视觉补充未完成”，两路失败无正文时不生成空草稿；确认页证据面板显示“OCR 文字/视觉观察”来源标签（方法见 `tests/acceptance/BUG-006-multimodal-diagnostics-and-vision-fusion-2026-08-05.md`，Android/iOS 分别验证，并继续覆盖 `IMAGE-001` 三种识图模式与 `OCR-003` 真实图片质量）。
4. 复测 `IMPORT-006`（本轮关键）：在外部终端执行 `flutter build apk --debug` 并 `adb install -r` 到模拟器（Codex 沙箱构建卡死）；粘贴带配图的小红书/抖音链接，重点确认 Logcat 中 `[AIRecipe][CoverImage]` 每张图回传的 `status=` 值（如 `status=403` / `status=200`），用于判定是模拟器网络拒绝还是客户端解析问题；同时确认残缺图片 URL（`ci.xiaohongshu.com/?imageMogr2/...`）已不再出现在下载列表；随后按验收方法复测确认页配图、保存后详情页轮播、重启恢复、断网降级（方法见 `tests/acceptance/IMPORT-006-remote-covers-2026-08-03.md`）。
5. 复测 `IMPORT-007`：OCR 已安装时粘贴图文链接，确认经过图片识别阶段且草稿整合正文与 OCR 文本；OCR 未安装时确认有正文仍可生成、纯图片提示安装；断网/图片失效确认稳定中文错误（方法见 `tests/acceptance/IMPORT-007-link-import-ocr-2026-08-03.md`）。
6. 复测 `BUG-003`：建议优先使用带完整分享参数（含 `xsec_token`）的小红书链接创建任务；若使用无参数样本，预期看到“未获取到目标笔记内容…”或“平台未返回笔记内容（安全验证/失效）…”且任务可重试。
7. 复测 `IMPORT-004`：完成一次导入进入草稿确认页，验证“重新生成”按钮、二次确认、busy、新草稿替换、失败保留旧草稿、重启后禁用提示，随后保存验证数量减少与重启恢复（方法见 `tests/acceptance/IMPORT-004-regenerate-import-draft-2026-08-02.md`）。
8. 复测 `IMPORT-005`：新建/编辑菜谱从相册多选封面图，验证缩略图、首图“封面”标记、删除清理、详情页轮播、强停重启恢复与副本隔离（方法见 `tests/acceptance/IMPORT-005-recipe-cover-images-2026-08-02.md`）。
9. 成功时继续评分菜名、食材、用量、步骤、低置信度与人工修改数，并归档 AI 质量日志。
10. 项目负责人反馈后，Codex 归档结果并决定 `BUG-003`、`IMPORT-004`、`IMPORT-005`、`IMPORT-006`、`IMPORT-007` 进入 `DONE` 或返回 `DOING`；随后继续 `BUG-002`、`AI-003`、`SPK-003` 和 `SPK-002`。iOS 仍未验证。

## 已知限制

- `BACKUP-002`/`BACKUP-003`/`BACKUP-004`/`BACKUP-005` 已实现导出与导入（含替换/回滚），但均等待项目负责人验收；真实 `.airecipe-backup` 创建与恢复链路已可操作，验收未通过前不视为完成。
- 备份文件写入应用文档目录 `backups/`；「另存为」出口已实现（ADR-0036）：创建成功后可把备份导出到用户选择的位置（如下载），成功摘要同时展示完整路径；Android 不再必须 adb 取走文件。file_picker `saveFile` 的 SAF 落盘仍需真机确认。
- 备份不含加密（首版未承诺）；备份含用户菜谱与图片，UI 已提示不要公开分享备份文件。
- 构建环境约束（2026-08-07 起）：flutter 命令须在 `C:\AIM`（junction → `code/apps/mobile`）下执行（impellerc 无法写中文路径）；`flutter clean` 后需先设代理（`127.0.0.1:7897`）再构建（sqlite3 hook 下载 GitHub 库，已缓存）。详见验收文档第 2.1 节。
- 导入 staging 清理账本已实现（成功/失败/放弃预检均清理会话目录，启动时扫除遗留目录）；回滚备份保留期限尚未定值，当前由用户手动管理，归 `BACKUP-006`。
- 备份加密只预留归档能力，首版方案不承诺加密；Android/iOS 文件选择、空间不足、后台中断、同一备份跨平台恢复与 1000+ 菜谱大库导入性能均未实机验证（归 `BACKUP-006`）。
- 多模态诊断已能区分连接失败、无首字节、正文卡住、Provider 408/504、SSE 未终止、图片协议不兼容、Schema 错误和图片断言失败（`BUG-006` P0-1 已实施，待真机验证）；精确 DNS/TLS 分阶段指标依赖平台能力，P0 最低要求（未取得响应头/已取得响应头但无首字节/正文未结束/解析失败）已满足。
- 多图多模态识别已实施（`IMAGE-002`，ADR-0029）：多模态只转录图片文字，纯 LLM 分组判断（独立→多草稿 / 合成→单草稿），失败回退合并；链接与手动图片均默认多模态优先、多模态不可用时回退本地 OCR（2026-08-07 修订）。已知限制：分组判断失败回退为全部合并；多图独立时作者正文仅并入第一份草稿。`BUG-006` P0-2 的 OCR+多模态融合处理器保留但不再自动启用。
- 服务器端未实现，不应把“登录用户”描述为已经具备真实登录或云同步。
- OCR Android 路径尚未通过真实 PP-OCRv5 模型与真机质量验收；iOS Runtime 未实现，`SPK-002` 不能标为完成。
- 真实小红书/抖音图片、视频到最终菜谱的端到端链路仍受平台可访问性和真实 Provider 影响；无完整分享参数（`xsec_token`）的 `explore` 链接会被平台返回推荐页而非目标笔记，本轮已把该情况分类为可操作中文错误且可重试；带完整分享参数的链接仍可能在解析器或 LLM 阶段失败。
- 人工粘贴正文已通过一次真实 OpenAI-compatible 纯文本服务兼容验证，但样本不是固定平台事实样本，菜名、食材、用量、步骤、低置信度和人工修改数仍未评分。
- Android 单张截图选择器已接入；真实 PP-OCRv5 与 LLM 的完整 OCR→菜谱端到端质量仍未验证。视频选择/ASR、Gemini、本地兼容服务和 iOS 仍未接入或验证。
- 当前工作树含并行 UI HTML 与 OCR 改动；后续 Flutter UI 实现不得修改或回滚既有 `design/` 和 OCR 交付。
- 当前机器直接调用 Flutter CLI 在受限沙箱内可能因 SDK 缓存权限无输出并超时；质量门使用同一 Flutter SDK 在获准的外部执行环境中完成。
- 导入证据 `ImportContent` 当前仅在本次执行会话内传递；应用重启后可恢复待确认菜谱草稿，但不能恢复完整原文片段。字段与原文/OCR/ASR 证据尚无一一映射，UI 会明确提示且不会伪造来源。
- 正式 `Recipe` 当前只保存 `sourceId`，尚无来源 URL、canonical URL 或不可变来源快照契约；导入任务清理、导出或未来同步前必须补齐该数据契约。
