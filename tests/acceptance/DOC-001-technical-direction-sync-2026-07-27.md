# DOC-001：技术方向长期事实源同步验收

- 日期：2026-07-27
- 任务：`DOC-001`
- 结论：通过

## 验收范围

将本轮确认的 Flutter、OCR、本地 LLM 网络安全与 Git 状态同步到项目长期文档，避免后续会话重新选型或重复初始化。

## 验收项

| 验收项 | 结果 | 证据 |
|---|---|---|
| 产品需求文档更新到 v0.3 | 通过 | Flutter、PaddleOCR 与三种本地 LLM 连接模式已经写入产品需求和版本记录 |
| 永久协作规程移除过时框架比较 | 通过 | `AGENTS.md` 将框架任务改为 Flutter 关键能力验证 |
| 根目录导航同步架构文档 | 通过 | `README.md` 已链接移动端、OCR 与本地 LLM 网络安全文档 |
| 技术决策可长期追踪 | 通过 | `ADR-0008`、`ADR-0009`、`ADR-0010` 已生效 |
| 技术探索未被误标完成 | 通过 | `SPK-001`、`SPK-002`、`SPK-003` 仍为 `TODO`，等待工程与真机数据 |
| Markdown UTF-8 检查 | 通过 | 全仓库 Markdown 未发现编码损坏产生的问号字符 |
| 本地 Markdown 链接检查 | 通过 | 共检查 47 个 Markdown 文件，缺失本地链接 0 个 |
| Git 状态复核 | 通过 | 分支 `main`；无远程、无提交、无推送；项目文件仍未跟踪 |

## 已知限制

- 当前 Windows 环境不能完成 iOS 构建和 iPhone 真机验证。
- PaddleOCR、移动端 LAN/私有 VPN/HTTPS 路径仍需执行对应 Spike。
- Git 用户级全局 ignore 文件有权限警告，但不影响项目内 `.gitignore` 和仓库状态。

## 后续更正

2026-07-27，项目负责人澄清 LLM 连接只采用“API 地址 + Key + 协议 Adapter”方案。本文关于三种 LLM 网络连接模式的验收内容已由 `ADR-0011` 和产品文档 v0.4 取代；本节保留用于记录当时状态。
